# Generated / vendored — never format (avoids churn that fights the toolchain).
pnpm-lock.yaml
**/dist/
**/dev-dist/
**/coverage/
**/*.min.*
.obsidian/**
.claude/**
# Mockups that inline the app's real stylesheets (mockups/tools/inline-app-css.mjs).
# Most of the file is generated css copied verbatim from frontend/src, so
# formatting it would reflow the generated block and fight the generator on
# every run. The tool's output is the canonical form.
mockups/map-embedded-v1.html
mockups/map-reach-v1.html
mockups/map-split-v2.html
mockups/map-chrome-v1.html
mockups/map-search-v1.html
mockups/map-google-pins-v1.html
# Google's published CBMS stylesheet schema, committed verbatim so the map styles can be
# validated offline (developers.google.com is unreachable from sandboxed sessions).
# Reformatting a vendored file would hide any drift from Google's own copy.
docs/design/map-styles/cbms-json-schema.json
