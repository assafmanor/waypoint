# Architecture Decision Records (ADRs)

This is the project's memory. Docs elsewhere describe _what is true now_; ADRs preserve _what we decided and why_, so decisions are never silently reversed or re-litigated.

## What earns an ADR

Any decision that is expensive to reverse or that a future contributor would otherwise question: product scope calls, platform/tech choices, data-model shapes, collaboration rules, integration boundaries. If you'd want to know "why is it like this?" six months from now, write one.

## Process

1. Copy the template below into `NNNN-short-title.md` (next number).
2. Fill it in. Keep it short — a screen, not an essay.
3. Set **Status**: `Proposed` → `Accepted` → (later) `Superseded by NNNN`.
4. Add it to the index below.
5. Never edit an Accepted ADR's decision; instead write a new ADR that supersedes it.

## Template

```markdown
# NNNN — Title

**Status:** Proposed | Accepted | Superseded by NNNN
**Date:** YYYY-MM-DD

## Context

What forces are at play? What problem are we deciding on?

## Decision

What we chose.

## Consequences

What becomes easier, harder, or constrained as a result.

## Alternatives considered

What else we weighed and why we didn't pick it.
```

## Index

| #                                                               | Title                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | Status             |
| --------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------ |
| [0001](0001-adopt-document-everything.md)                       | Adopt "document everything" + docs handbook + ADRs                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | Accepted           |
| [0002](0002-each-member-own-google-account.md)                  | Each member connects their own Google account                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | Accepted           |
| [0003](0003-one-way-calendar-sync.md)                           | Calendar sync is one-way (trip → personal)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | Accepted           |
| [0004](0004-integrations-are-pipes.md)                          | Integrations are pipes, not screens                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | Accepted           |
| [0005](0005-peers-not-roles-v1.md)                              | Roles in v1: `admin` (creator) + `peer`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | Accepted           |
| [0006](0006-no-live-location-v1.md)                             | Own-device location in v1; member sharing deferred                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | Accepted           |
| [0007](0007-platform-pwa.md)                                    | Platform: mobile-first PWA                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | Accepted           |
| [0008](0008-backend-supabase.md)                                | Backend: traditional self-owned Node/TypeScript service                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | Accepted           |
| [0009](0009-docs-english-ui-hebrew.md)                          | Docs in English, product UI Hebrew/RTL                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | Accepted           |
| [0010](0010-repo-vs-internal.md)                                | Keep private material out of the repo                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | Accepted           |
| [0011](0011-hard-soft-event-model.md)                           | Hard/soft event model as the core primitive                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | Accepted           |
| [0012](0012-conflict-lww-undo.md)                               | Conflicts: last-writer-wins + undo for v1                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | Accepted           |
| [0013](0013-google-only-auth-v1.md)                             | Google-only authentication for v1                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | Accepted           |
| [0014](0014-budget-display-only-v1.md)                          | Budget is display-only in v1                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | Accepted           |
| [0015](0015-document-encryption-server-side.md)                 | Document encryption: server-side at rest                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | Accepted           |
| [0016](0016-plan-trip-modes-one-surface.md)                     | Plan/Trip modes = one surface; auto-by-date switch + manual override                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | Accepted           |
| [0017](0017-mobile-first-device-targets.md)                     | Mobile-first; phone-primary device targets (tablet 2nd, desktop minimum)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | Accepted           |
| [0018](0018-timeline-data-model-shape.md)                       | Timeline & data-model shape (drop Day, no stored `now`, `endDate`, client ids, roles)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | Accepted           |
| [0019](0019-sync-protocol.md)                                   | Sync protocol: monotonic change log, atomic write path, snapshot + catch-up                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | Accepted           |
| [0020](0020-auth-session-architecture.md)                       | Auth & session architecture (memory JWT + rotating refresh, single-origin, `AuthIdentity`)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | Accepted           |
| [0021](0021-multi-trip-membership.md)                           | Multi-trip membership & active-trip client state                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | Accepted           |
| [0022](0022-control-plane-vs-data-plane.md)                     | Control plane vs. data plane: only the data plane routes through ChangeService                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | Accepted           |
| [0023](0023-zod-first-entities-and-openapi.md)                  | Zod-first entity shapes; OpenAPI generated from schemas                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | Accepted           |
| [0024](0024-app-shell-and-trip-lifecycle.md)                    | App shell & trip-lifecycle navigation (thin shell; login/zero-state/create/join/switcher)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | Accepted           |
| [0025](0025-trip-mode-edit-capability-tiers.md)                 | Trip-mode edit capability tiers & the Plan escape (refines 0016)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | Accepted           |
| [0026](0026-real-clock-and-dev-time-travel.md)                  | Real clock as the time source + dev time-travel                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | Accepted           |
| [0027](0027-soft-item-lifecycle-shelf-slip.md)                  | Soft-item lifecycle: derived phases, slip/Do-it-now, shelf as a parking lot                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | Accepted           |
| [0028](0028-plan-violet-color-budget-dark-ready.md)             | Plan violet + Night/Day mode identity, semantic color budget, dark-mode-ready tokens                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | Accepted           |
| [0029](0029-trip-mode-day-scope-gating.md)                      | Trip-mode day-scope: verb gating for past and future days (refines 0025, 0027)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | Accepted           |
| [0030](0030-join-is-link-only.md)                               | Joining a trip is link-only — no short invite codes (paste-a-link covers app-first arrivals)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | Accepted           |
| [0031](0031-hosting-on-railway.md)                              | Hosting: Railway, one project (single-origin service, Postgres; buckets later)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | Accepted           |
| [0032](0032-minimal-trip-creation.md)                           | Trip creation is minimal: destination → dates → auto-suggested name; currency/budget/timezone derived or deferred                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | Accepted           |
| [0033](0033-all-trips-home.md)                                  | "All trips" home page: land there unless a trip is live; replaces the switcher sheet, create-only                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | Accepted           |
| [0034](0034-document-encryption-trust-model.md)                 | Document encryption trust model: server-side-at-rest does not protect against the operator (refines 0015)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | Accepted           |
| [0035](0035-in-app-back-and-return-gesture.md)                  | In-app "back" is one guarded history step; the PWA return gesture triggers it (Home-anchor tabs, overlays in history)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | Accepted           |
| [0036](0036-event-time-setter.md)                               | Event time-setter: quantized 15-min quick-pick + typeable exact fallback; end as duration; same-day only                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | Accepted           |
| [0037](0037-overnight-events.md)                                | Overnight events: end may run to 07:00 next day, filed under the start night; transportation is a separate category (supersedes 0036 §5)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | Accepted           |
| [0038](0038-icons-and-canonical-category.md)                    | Curated event/item icon set + a canonical `category` primitive (icon is a badge override; BookingType + map-pins derive from category)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | Proposed           |
| [0039](0039-trip-settings-admin-governed-data-plane.md)         | Trip settings: admin-governed edits; settings mutations join the data plane (refines 0005/0032; partially supersedes 0022)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | Accepted           |
| [0040](0040-trip-mode-access-window-and-past-trip-archive.md)   | Trip mode is a live-window-only state (override one-directional); a finished trip is a read-only archive (refines 0016/0025/0029/0033)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | Accepted           |
| [0041](0041-parallel-overlapping-events.md)                     | Parallel/overlapping events: a containment forest rendered as nests + clusters; Trip quiet, Plan flags with "הזז" resolve; board shows one hero + "ועוד N" (refines 0011/0018/0028/0036)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | Accepted           |
| [0042](0042-shared-state-is-offline-syncable.md)                | Everything shared between people is offline-syncable (read-cache + outbox + device-wide reconnect flush); server-only actions block offline (refines 0019/0022/0039)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | Accepted           |
| [0043](0043-day-view-now-line-phases-and-archive-chrome.md)     | Day view: now-line + derived-phase presentation, phase-scoped verbs (Done reframed, adaptive nudge, quick-add), archive chrome (past-day wash + today-anchored strip), Plan now-reference (refines 0011/0025/0027/0028/0029/0040)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | Accepted           |
| [0044](0044-settling-a-finished-trip.md)                        | Settling (Done/Skip/Restore) extends to a finished trip: a read-only _structural_ archive that stays settle-editable; archive-presentation design is a follow-up (refines 0040/0043)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | Accepted           |
| [0045](0045-trip-home-real-data-only.md)                        | Trip-mode Home shows real data only: quick-access reworked to three real shortcuts (next code / WiFi / documents; navigate-to-next deferred to the maps work), the weather/FX/budget glance row replaced by a derived day-at-a-glance card (refines 0004/0006/0014)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | Accepted           |
| [0046](0046-retire-the-task-board.md)                           | Retire the gitignored task board: ADRs + session notes are the record; decided-but-unbuilt work becomes a flat committed `docs/backlog.md` (partially revises 0010)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | Accepted           |
| [0047](0047-booking-event-linkage-and-notes.md)                 | Booking↔Event linkage is strict 1:1 optional (hotel spans reuse `endDate`); merged edit surface; explicit delete/unlink prompt; per-file grouped documents; booking notes + hotel WiFi in `Booking.details`, replacing `TripNote` wifi (refines 0004/0011/0018/0025/0045)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | Accepted           |
| [0048](0048-index-build-data-model-refinements.md)              | Index-build data-model refinements: Event is the sole time authority (drop `Booking.startsAt`/`endsAt`); a minimal `Place` entity (all `placeId`s become FKs); transport gains origin/destination Place FKs; `TripNote` retired; `kind` stays selectable on booked events (refines 0047/0018/0038/0037/0011/0004)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | Accepted           |
| [0049](0049-index-tab-mode-and-lifecycle.md)                    | The Index tab across mode and lifecycle: mode changes chrome only (identical content/capability); a during-trip past/upcoming split; a read-only, washed archive with documents still openable; bookings and documents are peers with matched add affordances (refines 0016/0040/0044/0028/0047)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | Accepted           |
| [0050](0050-home-quick-access-deep-links-and-empty-states.md)   | Home quick-access tiles deep-link into the Index; derived tiles (next code, WiFi) are absent when there's no source (grid reflows, no manual add), the managed documents tile is always present with a ＋ invite when empty (refines 0045/0047)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | Accepted           |
| [0051](0051-place-normalization-and-authority.md)               | Full Place normalization: location is expressed only via `Place` (drop `Event.location`/`Booking.address`); a linked event's place lives on its booking (its own `placeId` is cleared); `Event.bookingId` made `@unique` to enforce the 1:1 (revises 0048 §2/§3; refines 0047/0038/0003)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | Accepted           |
| [0052](0052-document-lifecycle-view-manage-and-feedback.md)     | Document lifecycle: mobile-first viewing (open/download a PDF, no sole-path iframe); delete/rename/replace at both layers; upload + load feedback via one shared spinner; cause-aware errors + pre-upload validation; four visually distinct type badges (refines 0047/0049/0015/0034/0017)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | Proposed           |
| [0053](0053-index-booking-detail-view-and-merged-edit-reach.md) | Index bookings get a read-only detail view + "⋯" menu → edit/delete (parity with the event card); the merged edit surface (ADR-0047 §2) becomes reachable from the linked event, not just the Index (revises 0049; completes 0047 §2; refines 0011/0043)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | Proposed           |
| [0054](0054-ambient-span-events-off-the-day-schedule.md)        | Ambient-span events (lodging / multi-day bookings, `endDate` set) are backdrop, not counted schedule blocks: excluded from the day-at-a-glance rail + `remaining` count, rendered across their whole span (refines 0045/0041/0018/0047/0037/0011)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | Proposed           |
| [0055](0055-document-blob-read-caching.md)                      | Document blob read caching: a read-through, ciphertext-only cache — server two-tier (in-memory LRU + local FS) below `getObject`, plus a client Cache-API read cache; closes the offline-reads gap (refines 0015/0034/0031/0042)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | Accepted           |
| [0056](0056-faster-document-uploads.md)                         | Faster document uploads: optimistic upload via the offline outbox (close the sheet instantly, queue the file blob, background flush) + idempotent server re-POST; Redis write-buffer / presigned direct upload deferred (refines 0042/0018; relates to 0015/0034)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | Accepted           |
| [0057](0057-documents-join-live-sync-and-offline-cache.md)      | Documents join the live sync path + offline read cache: WS `document` changes patch the section live, `applyChangeToCache` mirrors `db.documents`, and a peer replace evicts the client blob cache to avoid stale bytes (refines 0042/0049; relates to 0019/0055/0056)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | Superseded by 0058 |
| [0058](0058-documents-in-the-trip-snapshot.md)                  | Documents move into the `TripSnapshot` and become a first-class reactive list like bookings (one load/cache/live path); supersedes 0057 — removes the emitter, keeps the peer-replace blob-cache eviction (refines 0049; relates 0019/0042/0055)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | Accepted           |
| [0059](0059-booking-presentation-on-home-and-index.md)          | Booking presentation across the Trip-mode Home & Index: the hero surfaces a booking at its transition moments (hotels around check-in/out, transport at departure/arrival) not across its whole span, a distinct "you're inside a booking now" treatment, and one shared appearance grammar for hero/row/detail (refines 0045/0054/0053/0049/0011/0004)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | Accepted           |
| [0060](0060-reopen-after-idle-returns-to-trip-home.md)          | Reopening after ≥~30 min hidden resets Trip mode to the Home tab on today (closes the warm-resume gap; under 30 min resumes in place; Plan mode preserves position) (refines 0033/0024/0035)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | Accepted           |
| [0061](0061-plan-home-readiness-rework.md)                      | Plan-mode Home "what's missing" rework: four existing checks kept with CTAs that do-the-thing + one new per-traveller documents/passports check (post-0058); completed checks collapse; readiness stays advisory (never gates go-live); real-data-only/derived (refines 0045/0004)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | Accepted           |
| [0062](0062-zoom-disabled-except-image-preview.md)              | Zoom disabled app-wide (double-tap via `touch-action: manipulation`; pinch via multi-touch gesture suppression since iOS ignores the viewport meta), except inside the image preview which opts back into pinch-zoom + pan (refines 0007/0017/0052)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | Accepted           |
| [0063](0063-category-time-behaviour-profile.md)                 | A per-`category` time-behaviour profile (`bracketed` + `ambientWhenMultiDay`), derived not stored: "prolonged" events show start & end with a passive middle, multi-day ones go ambient — one closed lookup beside the icon registry that every time surface reads, so 0059/0054 become applications not per-type special-cases (refines 0038/0054/0011/0018)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | Accepted           |
| [0064](0064-day-transition-entries-and-home-band-trim.md)       | Per-day transition entries for multi-day bracketed bookings (a check-in/departure row on the start day, a check-out/arrival row on the end day, derived via one shared `bookingTransitionsOnDate`), the backdrop strip trimmed to strictly-middle nights, and the persistent ambient band dropped from the Home glance with the mid-stay treatment gated inside the span (refines 0059/0054/0063/0053/0043)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | Accepted           |
| [0065](0065-app-scope-many-trips-small-groups.md)               | App scope: Waypoint is a multi-trip, multi-user app whose per-trip group is small (~5) — "~5" sizes one trip's group, never the app; invite-only is a privacy choice not a user cap; not production-scaled yet but a grow-later posture, not capped (clarifies vision/PRD/overview/tech-stack/CLAUDE.md + the "~5" phrasing in earlier ADRs; per-trip sizing in 0005/0012/0019/0022 stands; scopes 0034's operator-trust to a single self-hosted group)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | Accepted           |
| [0066](0066-client-local-data-teardown-on-signout.md)           | Client local data (Dexie tables, active-trip id, decrypted document blob cache) is wiped on sign-out and on genuine online session-loss; the offline cold-boot fallback is exempt (keeps its cache). Device-local hygiene closing the frontend-review F-01 cross-session leak on shared devices; not client-side encryption — the multi-tenant trust question still defers to 0015/0034/0065 (relates 0020/0034/0042/0055/0058/0065)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | Accepted           |
| [0067](0067-revocable-code-invites-and-removal-blocks.md)       | Revocable short-code invites + removal blocks: the invite is a durable per-trip `Invite` row whose short code (8-char base58) is itself the credential in the link — no HMAC token; one stable get-or-create link per trip with an admin rotate/revoke, valid until the trip ends; a `TripBlock` written on an admin kick stops rejoin via a live link, with an admin "Removed" list to allow back; `GET /invites/:code` returns `tripId` so an existing member is redirected into the trip (supersedes 0030; refines 0020/0024/0005/0039; closes backend-review B-07)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | Accepted           |
| [0068](0068-sync-cursor-commit-consistency.md)                  | `Change.seq` is a commit-consistent watermark: writes serialize on a per-trip Postgres advisory lock so seq order == commit order, and `getSnapshot` reads `latestSeq` first at RepeatableRead — closing backend-review B-01 (a client advancing its cursor past a concurrently-committed change it never receives). Promotes ADR-0019's deferred "per-trip advisory lock" to the implemented mechanism (refines 0019; relates 0042/0065)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | Accepted           |
| [0069](0069-document-download-only-and-mime-allowlist.md)       | Documents are download-only with a server-enforced upload MIME allow-list: `/content` sends `Content-Disposition: attachment` + `nosniff`, uploads outside `ALLOWED_DOCUMENT_MIME_TYPES` (PDF + raster images) are 415'd before storage, and the viewer only opens PDFs inline — closing backend-review B-03 (uploaded HTML/SVG running script in the app origin → co-traveler account takeover). Refines 0052 (narrows "open"); adds in-transit/in-browser handling to 0015/0034                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | Accepted           |
| [0070](0070-global-error-envelope-and-temporal-validation.md)   | One global exception filter puts every error in the documented `{error:{code,message}}` envelope (guards, `NotFound`, mapped Prisma `P2002`/`P2025`/`P2003`) and folds in the SPA-shell fallback; `packages/shared` gains domain-typed `date`/`startsAt`/`endsAt`/`timezone` so malformed input is a 400 not a 500 — closing backend-review B-05 (relates 0019/0023/0042)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | Accepted           |
| [0071](0071-fail-fast-config-validation.md)                     | Fail-fast config validation in `bootstrap()` (base64-32-byte keys, Google vars present, URLs parse; value-free errors, non-zero exit) and refuse to boot with `DEV_AUTH=1` under `NODE_ENV=production` (plus `isDevAuthEnabled()` gating the bypass) — closing backend-review B-04 (relates 0020/0031/0015/0034)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | Accepted           |
| [0072](0072-graceful-shutdown-and-readiness.md)                 | Graceful shutdown (`enableShutdownHooks()`; `SyncGateway` closes sockets + the WS server on SIGTERM) and a readiness split: `/health` stays static liveness, `/health/ready` does a `SELECT 1` (503 when the DB is down) and becomes the Railway deploy gate — closing backend-review B-08 (refines 0031; relates 0019)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | Accepted           |
| [0073](0073-growth-facing-sync-and-membership-hardening.md)     | Growth hardening: `GET /changes` paged at `CHANGES_PAGE_LIMIT` (client loops from the last seq), `@@index([userId])` on Membership, and last-admin promotion folded into the removal transaction (race-safe under the ADR-0068 lock) — closing backend-review B-09 (refines 0019/0068/0039)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | Accepted           |
| [0074](0074-evict-removed-member-websocket.md)                  | Removing a member (or deleting a trip) closes their live WebSocket server-side (`SyncGateway.disconnectUser`/`disconnectTrip`, WS 1008) so a removed member stops receiving changes immediately — WS membership was only checked at upgrade. Closes backend-review B-02 (relates 0019/0020/0039)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | Accepted           |
| [0075](0075-rate-limiting.md)                                   | Endpoint-specific rate limiting (`@nestjs/throttler`): one generous per-IP default (300/60s) so offline reconnect bursts aren't 429'd, tightened to 20/60s on the abuse targets (auth/refresh, join, invite-preview); trust `X-Forwarded-For` only from Railway's proxy in prod — closing backend-review B-10 (relates 0024/0031/0070/0065)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | Accepted           |
| [0076](0076-auth-and-document-low-severity-hardening.md)        | Low-severity hardening: cross-tab refresh single-flight via a Web Lock (B-11), reject Google `email_verified=false` + provision User/AuthIdentity in one transaction (B-12), and validate document `ownerUserId` is a trip member (B-13); orphan-blob sweep + change-`after` standardization + email-change policy documented as deferred (relates 0020/0015/0034/0019)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | Accepted           |
| [0077](0077-unified-glance-rail-annotation-grammar.md)          | One glance rail-annotation grammar: a single pill primitive where tint carries family (amber time-anchor / neutral structure) and the connector carries shape (bar+feet = a span of a booking's two same-day edges, stem = a single-instant point, tick = a block note); time-anchors above the rail, structure below; a multi-day hotel degrades to a point per edge day, a same-day flight/ferry is a span; overlap/containment fold into the neutral note; a legs-line collapse past two lanes; edge anchor-in + short-span handling (refines 0059/0054/0064/0041/0045/0063/0028)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | Accepted           |
| [0078](0078-feedback-state-family.md)                           | A shared feedback-state family (`EmptyState`/`ErrorState`/`LoadingState`+`Skeleton`/`StatusBanner`) under `ui/feedback/` owning the SHELL, screens passing CONTENT; chrome-preserving (renders inside `AppShell`); the ~6 bespoke empty shells collapse to one, loading gains skeletons + reduced-motion, the error gets an optional retry, status/offline get one toned banner on the status tokens (not amber/teal/plan). New files only, no screen migrated; `SyncBadge` left for the sync track. Implements UI/UX review U-10 (builds on 0077; reuses the 0052 Spinner; relates 0028)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | Accepted           |
| [0079](0079-single-modal-primitive.md)                          | One `Modal` overlay primitive with `sheet` + `dialog` variants sharing the overlay-stack registration (ADR-0035) + focus contract (F-08); trap defaults off for `sheet` (nested prompt reachability) and on for `dialog`; `Sheet` becomes a thin wrapper, its consumers unchanged; the `.confirm-*`/`.event-form-*` families fold on in Wave 2 (closes the substrate for review U-01/U-02; relates 0035/0028)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | Accepted           |
| [0080](0080-per-entity-sync-status.md)                          | Per-entity sync status: one `synced/pending/failed(reason)` model per entity derived frontend-only from the outbox (id-keyed via `outboxOpEntityId` + an in-memory pending index) and the F-03 failed store; `useSyncStatus(entityId)` + a non-color-coded `SyncBadge` + a persistent review/retry (dead-letter) sheet replacing the timed failed-badge, so a rejected write is recoverable not silently lost; DocumentsSection is the reference wiring, bookings/events migrate in Wave 3. Implements UI/UX review U-04 (builds on F-03; relates 0077/0078/0019/0042)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | Accepted           |
| [0081](0081-group-change-feed.md)                               | A quiet group change-feed: a bounded (last 20), WS-fed recent-changes buffer that NARRATES peer edits off the same `change` stream in `applyRemoteChange` (never re-applies, no second socket), attributed via F-05's real `actorUserId` + the roster, plus a dismissable `ChangeFeed` domain strip on the Trip-mode Home below the board. Own edits filtered out; polite live region; auto-collapses when empty; neutral chrome (not amber/teal/plan). Implements UI/UX review U-09 / PRD §4.2 (relates 0019/0004/0028; depends F-05)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | Accepted           |
| [0082](0082-adopt-non-color-design-tokens.md)                   | Adopt non-color design tokens (`--space-*`, `--text-*`/`--leading-*`, `--radius-*`, `--elevation-*`, `--bp-*`, safe-area, sync/status) in `tokens.css`, matching the documented spacing/type/radius/elevation ramps 1:1 — the ramps were prose-only, so 6.5k lines hard-coded and drifted; defined-not-migrated (primitives born on them, screens convert opportunistically), unblocking dark mode. Implements UI/UX review U-08 (extends 0028; relates 0017/0065)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | Accepted           |
| [0083](0083-whenfield-datetime-standard.md)                     | WhenField — one date/time entry standard: a `day` variant (native date + amber TimePicker) and a `span` variant (two [native date + compact tap-to-open time] endpoints across any two days, `+N` badge + duration read-out, uncapped), replacing the cramped native `datetime` box that clipped the date and showed AM/PM. Bakes in four rules (no squeezed native control, one time grammar, when-block first, panels auto-close + keyboard-safe); removes `DateTimeField` so no misusable primitive remains. Supersedes U-05's `datetime` mode (reuses/generalizes 0036/0037; relates 0028/0017/0047/0048)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | Accepted           |
| [0084](0084-booking-duration-display.md)                        | Booking duration display: add a `durationUnit` (`hours`/`nights`/`auto`) to the ADR-0063 category time-behaviour profile + a derived `eventDurationUnit` and one shared `formatBookingDuration` helper every preview reads (detail view `משך` fact + Index row) — transport → hours (even overnight), lodging → nights, else auto (hours same-day, days when it spans). Derived, keys on `category`, no per-type branching. Extends 0063 (relates 0059/0053/0038)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | Accepted           |
| [0085](0085-relative-day-phrasing.md)                           | Relative-day phrasing: the Index booking rows name a booking's day relative to today (`relativeDay` → מחר/מחרתיים/עוד N ימים ahead, אתמול/שלשום/לפני N ימים behind) instead of a trip day-number, and the five countdown surfaces (board hero next-event, trip-list chip, header "יוצאים", join ticket, Plan departure) swap bare "יום/יומיים" for מחר/מחרתיים via `countdownParts`; the hero reads the event's calendar day (not its minute-count) past a day. Durations stay counts; `nightCount(1)` reads "לילה אחד". Refines 0084 (relates 0053/0059/0045/0028)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | Accepted           |
| [0086](0086-document-upload-pick-control-redesign.md)           | Document upload pick control redesign: replace the raw native `<input type="file">` (the LTR "Choose File" eyesore) in `DocumentUploadSheet.tsx` with a designed drop-zone that states the accept+size contract up front, a first-class camera-capture path (`accept=image/* capture=environment`, phone-first), and a picked-file preview card (image objectURL / PDF tile + name + type·size + ✕ clear); the title-row icon tracks the chosen type badge. Off-screen real input keeps the accept filter + all wiring; frontend + `he.ts` copy only, no backend/shared/data-model change; ADR-0052's spinner/progress/error/badges reused unchanged. Shipped as reusable primitives — `FilePicker` + `ChoiceGrid` (the latter now shared by the booking + both document type selectors, deleting the legacy `.bs-typesel`), `lib/bytes.formatBytes`, and a `busy` `FormActions`. Refines 0052 (relates 0017/0028) — mockup `mockups/document-upload-v1.html`                                                                                                                                                                                                                                                                                                                                  | Accepted           |
| [0087](0087-app-logo-waypoint-marker.md)                        | App logo: the **Waypoint marker** — a map marker (teal = place) with a glowing amber core (amber = the live "now"), drawing the product's name and reading as travel at a glance, inside the ADR-0028 color budget (the two hues combine on purpose: a place that knows what's next). Always on a contrasting ground (dark board/indigo tile or light paper), never teal-on-teal. Regenerates `favicon.svg` (+ a circular `icon-mark-bright.svg` for the clipped inline Login/Join marks) and the PWA/apple-touch PNGs from one geometry; frontend/assets only, no manifest wiring or backend change. Direction B (Board Pin) kept on record, not shipped. Relates 0028/0017/0007 — mockup `mockups/logo-v1.html`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | Accepted           |
| [0088](0088-home-wifi-tile-gated-to-the-stay.md)                | Home WiFi quick-access is gated to the hotel stay: the derived WiFi tile shows only while checked in (`check-in ≤ now < check-out` of the hotel booking's linked stay event), instead of the whole trip; a hotel with WiFi but no scheduled stay falls back to showing (no window to gate). `hotelWifi` moves to a pure, unit-tested `lib/home-quick.ts` derivation. Frontend + derivation only, nothing stored. Refines 0050/0047 (relates 0059/0064/0018)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | Accepted           |
| [0089](0089-past-booking-rows-drop-the-transition-verb.md)      | Past booking rows drop the transition verb: a booking's Index schedule line leads with the type's action word (המראה/נחיתה, צ׳ק-אין/צ׳ק-אאוט, יציאה/הגעה, התחלה/סיום) only while it's upcoming or in-progress; once it's behind you (`isEventPast` — the same edge `splitBookings` files on) the verb drops and the row reads day + time + duration alone (_אתמול · 53:30 שע׳_). In-progress (hotel mid-stay, flight in transit) keeps the verb; edge selection, relative day, time and duration all unchanged. `scheduleLabel` only, no data/backend touch. Refines 0053/0085 (relates 0059/0063/0049/0028)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | Accepted           |
| [0090](0090-back-is-computed-from-nav-state.md)                 | Back is computed from the current navigation state, not traversed through browser history — the durable replacement for ADR-0035's "in-app navigation _is_ history" mechanism (its behavior is kept). One pure `resolveBack(snapshot) → BackAction` is the whole layer-peeling policy; every trigger (edge gesture, Navigation-API system-back interceptor, nav-bar Home tap, shell back buttons) routes through it + one `runBack` executor; structural back is always an explicit `navigate(..., {replace})`, never `navigate(-1)`. Deletes `tabStep`/`structuralBackStep`/`systemBackDecision`/`homeBaseIdxRef`/`historyIdx`/`homeBehind`; in-trip history goes flat (tabs + day always `replace`). Non-Chromium desktop native-back is a documented graceful-minimum limit; installed iOS + Chromium fully covered. Supersedes 0035's mechanism (relates 0060/0079/0007/0017)                                                                                                                                                                                                                                                                                                                                                                                                              | Accepted           |
| [0091](0091-sync-badge-cloud-and-silent-when-synced.md)         | SyncBadge cloud iconography + silent-when-synced + full adoption: the per-entity marker becomes a chip-less cloud glyph via the shared `Icon` (`cloud-check`/`cloud-up`/`cloud-bang`), disambiguating from the day-view done ✓; a new connected `EntitySyncBadge` owns the one cross-surface policy — silent when synced everywhere (exception-only; `showSynced` escape hatch); `ListRow` gains a fixed reserved-width `sync` column so bookings + documents align, and the dense day-view card mounts the marker on the meta line (`flex-start`) so a long title never reflows. Shape still carries the state, color still only from `--sync-*`. Amends 0080's glyph choice (relates 0082/0078/0043/0028)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | Accepted           |
| [0092](0092-unsynced-treatment-and-change-groups.md)            | Unsynced treatment + change-group counting: a pending item now reads as provisional — the whole row/card dims (~0.6 opacity) until the write lands (driven by one `useUnsynced(id)` hook; failed stays full-opacity to keep calling for action); the header "N changes" summary counts pending change _groups_ (`withChangeGroup` stamps a shared `groupId` on the ops one user action enqueues) so a booking + the places backing its route is one change, robust to places becoming user-visible (replaces a brittle verb-classification pass); and a queued document upload is honest offline — the connected `cloud-up` marker + a static "ממתין להעלאה" instead of an active "מעלה…" spinner. Extends 0080/0091 (relates 0056/0042/0082)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | Accepted           |
| [0093](0093-offline-booking-linked-event-coherence.md)          | A booking's linked event is coherent offline, via one generic sync path: a timed booking saved/edited while offline now shows its schedule + lands on the timeline immediately (and survives a cold reopen), instead of appearing scheduleless until reconnect. The client owns the linked event's id (`BookingSheet` stamps the `event` seed — the existing event's on edit, a fresh UUID otherwise); the booking→event derivation lives once in `@waypoint/shared` (`bookingEventFields`, called by both the server's `eventDataFromBooking` and the client's `eventFromBookingSeed`, so they can't diverge); and the offline write emits the SAME `Change` a WS echo would (`bookingLinkedEventChange`), applied through the existing appliers (`applyChangeToCache` + a hoisted in-memory `applyEntityChange`) — so offline optimism and online sync share one path, with no bespoke reducer action or cache helper. `create` carries the full event, `update` only schedule fields (a merge preserves status/sortOrder). Same id → the echo reconciles in place on flush (no duplicate); online unchanged. Relates 0047/0048/0042/0018/0053                                                                                                                                               | Accepted           |
| [0094](0094-one-pluggable-change-applier-registry.md)           | One pluggable change-applier registry: each entity type declares how its changes apply, once, in a registry, so the appliers are table lookups with no per-type branching (adding a syncable, or moving one between stores, is a one-line edit). Memory channels (`state/trip-state.tsx`: event→reducer, trip/membership/booking/place/document→their stores via the generic `applyControlChangeToList`) and cache channels (`lib/cache.ts`: own Dexie table / `snapshotMeta` list / meta trip) are mirror registries — memory vs. persistence. Generalizes ADR-0093's shared `applyEntityChange` (its body was still an if/else). The offline cache mirror is collapsed onto the registry too — `applyOutboxOpToCache` maps a queued op to `Change[]` (`outboxOpToCacheChanges`) applied through the one `applyChangeToCache`, closing the membership `id`-vs-`userId` keying drift (offline resolves userId→id) and making a trip rename update the all-trips list on the WS path as well. Entity-type strings are a shared `ENTITY_TYPE` constant keyed off by backend + frontend. Events + maybe-items keep their undo-aware reducer path (ADR-0019); routing each verb's optimistic `setState` through the applier is the one remaining (optional) step. Relates 0093/0042/0019/0039/0058 | Accepted           |
| [0095](0095-named-constants-for-string-discriminants.md)        | Named constants for string discriminants (generalizes ADR-0094's `ENTITY_TYPE`/`CHANGE_ACTION`): a discriminant is declared once as `{ NAME: 'value' } as const` with a derived union and referenced by symbol everywhere, so a typo is a compile error and a rename is one edit. Placement follows the consumer — cross-layer vocabularies go to `@waypoint/shared` (`ERROR_CODE`: the exception filter + every service throw + the frontend `ApiError` predicates and `QUIET_DROP_CODES`; `WS_MESSAGE_TYPE`: both ends of the socket, payload shapes still per-side), frontend-only ones stay co-located with the type they feed (`OUTBOX_VERB` with `OutboxOp`, `TRIP_ACTION` with the reducer `Action`, `SYNC_STATE` with `SyncState`, `HTTP_METHOD` with the api client). Removes the hand-synced `MOVE_INTO_PAST`-style aliases in `lib/api.ts`. Wire-byte test mocks keep literals (contract under test); shared stays the source of truth for entity shapes, not client internals. Relates 0094/0018/0020                                                                                                                                                                                                                                                                              | Accepted           |
| [0096](0096-per-domain-claude-md-guides.md)                     | Per-domain `CLAUDE.md` guides (`packages/shared`, `backend`, `frontend`), additive to root `CLAUDE.md`: names the existing infra to reuse per layer (ChangeService/trip-scope guards/env constants/blob-cache on the backend; primitives/domain/feedback component layers + the ENTITY_TYPE-keyed sync registries on the frontend; the named-constants convention in shared) and the concrete anti-patterns ADR-0078/0079/0094/0095 each had to fix after the fact. Codifies reuse-existing-infrastructure-before-adding-new as a standing rule in root `CLAUDE.md`. Relates 0001/0046/0078/0079/0094/0095                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | Accepted           |
| [0097](0097-mockup-catalog-out-of-root-claude-md.md)            | Mockup catalog moves from an inlined ~16-entry list in root `CLAUDE.md` (loaded by every session regardless of task) to `docs/design/mockups.md`, routed through `docs/INDEX.md`'s Design table the same way ADRs are routed rather than inlined. Extends 0096's progressive-disclosure argument; content unchanged, location only. Relates 0096                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | Accepted           |
| [0098](0098-index-landing-and-dedicated-screens.md)             | The Index becomes a landing with two peer tiles (🎫 bookings, 📎 documents) that each push a dedicated full screen, instead of one scrolling page; the bookings screen gains a category filter chip row + search + past-bookings collapse (mirroring ADR-0061 exactly); every directional glyph moves to the real `NavArrow`/`Icon` SVGs (a `‹`/`→` Unicode arrow silently mirrors inside `dir="rtl"`); a motion pass (animated collapse, staggered filter reveal, tap feedback) replaces color/content as the polish lever. Back-to-landing reuses the existing overlay-stack (`useOverlay`) rather than a new route or `resolveBack` rule. No content types beyond bookings/documents are decided here. Refines 0049 §3/0090/0050/0061 (relates 0004/0011/0028/0096)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | Accepted           |
| [0099](0099-retire-the-custom-edge-swipe-gesture.md)            | Retires the custom edge-swipe return gesture (`ui/EdgeSwipeBack.tsx`, ADR-0035 §5) — deleted outright, not scoped/patched. Built building ADR-0098: the gesture's "start anywhere over an open overlay" relaxation assumed every overlay is a true full-bleed modal, an assumption the Index's routed-screen overlays broke (a swipe across the category chips got hijacked as back). Raised with Assaf, the call was broader than a scoped fix — no custom gesture surfaces in the app at all. Every other back trigger (system-back, tab taps, shell/dedicated-screen back buttons) is unaffected; the one real loss is iOS, which now has no gesture-based back at all (relies on explicit taps). Retires 0035 §5 (touches 0090 — one fewer trigger, mechanism unchanged)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | Accepted           |
| [0100](0100-index-bookings-header-search-redesign.md)           | Index bookings screen header/search/filter row redesign, reacting to post-ship "amateur"/"unpolished" feedback on the ADR-0098 build: back row + title/count row merge into one compact `idx-head` line (arrow beside "אינדקס", pointing right); the category chips + search become one denser row (label+icon+count per chip), search opening covers the chip strip in place with a small animation rather than opening a second row; the search icon is a minimal real SVG control (not emoji, not a circle); the chip row's edges fade (`scroll-snap` + a `mask-image` gradient) instead of hard-clipping a partially-visible chip; and the Index's own selected-chip/search-icon accent now follows Trip/Plan mode, extending ADR-0028's existing per-mode accent rule (same as the day-strip) rather than adding new color policy. Design decided via `mockups/index-bookings-compact-v2.html`; porting to the real component tree is tracked in the backlog, not done by this ADR. Refines 0098 §2 (touches 0028, relates 0099/0090)                                                                                                                                                                                                                                                     | Accepted           |

| [0101](0101-index-search-mode-and-header-titles.md) | Index bookings full-screen search mode: a new third `Modal` variant `'full'` (opaque, full-viewport, no backdrop-click-to-close) + a `SearchOverlay` primitive, entering search pops the keyboard (`initialFocusRef` on `useDialogFocus`), the search bar wears the trip header's mode-tinted chrome, and `IndexBackRow` takes a required `title`. ADR-0090's back-stack is unchanged — a `'full'` overlay closes by the same "close the topmost registered overlay" rule. Supersedes 0100 §3 (relates 0098/0090/0028) | Accepted |
| [0102](0102-search-mode-scope-and-multi-field-matching.md) | Search always searches every category (`CATEGORY_ALL`), matches by an extensible term array (title, confirmation code, singular+plural type label, synonyms) via a shared `packages/shared/src/search-terms.ts`, and a new local `frontend/src/lib/backPeel.ts` `peelBack(isModified, reset, close)` runs `reset()` instead of `close()` while a category filter is active — wired into both `useOverlay()` and `IndexBackRow.onBack` so tap/Escape/system-back peel the filter before leaving. Explicitly rejected adding a `BackAction` kind to `resolveBack` (it must stay a pure function of structural nav state). Refines 0101 (relates 0098/0090) | Accepted |
| [0103](0103-back-navigation-typed-layer-model.md) | Back navigation: keep ADR-0090's pure `resolveBack` + Navigation-API interception + flat-history core, but replace the single destructive LIFO overlay close-stack (its `pop()?.close()` orphans the filter-reset handler that stays mounted — the app-wide back inconsistency) with a **typed, non-destructive `BackLayer` registry** (`route`/`local-subview`/`repeatable-state`/`transient-overlay`/`exit-boundary`; handlers return `{handled, remainsActive}` so a repeatable layer stays registered); make `resolveBack(snapshot, trigger)` trigger-aware so system-back/app-button/Escape/explicit-close/forward each have one owner (Escape's independent close path removed); add a replace-written URL-param **restoration mirror** for Index local destinations (fragments rejected; history stays flat; Forward is a defined no-op for local layers); harden root native-exit + the two-tap arm→exit. Core shipped + device-validated on staging: the non-destructive registry fix, a fresh-document-load back-guard (the "sometimes back exits the app" cross-document case), and history-backed overlay markers so consecutive system-backs peel layers under the platform's user-activation gate. Refines 0090 (relates 0035/0079/0098/0099/0101/0102) | Accepted |
| [0104](0104-staging-environment-on-railway.md) | Staging environment: a second Railway **environment** in the existing project (not a second project — keeps ADR-0031's one-project posture), each with its own service instance, Postgres, and (later) Storage Bucket; a persistent `staging` git branch is the deploy source; `.github/workflows/ci.yml` gains a `workflow_call` trigger and a new `deploy-staging.yml` calls it first, deploying via the Railway CLI with a staging-scoped project token only on green — Railway's own git auto-deploy is turned off for that service so a red test run never reaches staging; secrets (`JWT_SECRET`/`TOKEN_ENCRYPTION_KEY`/`DOC_ENCRYPTION_KEY`), `DATABASE_URL`, and `S3_*` must never be copied verbatim from production. Builds on 0031 | Accepted |
| [0105](0105-loading-states-design.md) | Loading states as one language across the three loading moments (design + mockup, build tracked in `backlog.md`): **boot** (`shell.booting`) becomes the loud "board power-on" — dormant board warming up, the glow ramps as a warm-up (never the reserved live pulse), mono numerals — theme-aware but **not** mode-aware (theme is a device setting known at boot; mode is derived from an unloaded snapshot), so light = cool-paper (`--screen`) day ground + ink numerals + a small amber halo and dark = the night board amber-lit (**designed, not built** — rides the future `data-theme='dark'` toggle); **snapshot** (`snapshot.loading`) upgrades from anonymous shimmer to a **content-shaped skeleton** that pre-draws Home, the one tier needing a per-mode variant (Trip dark-board / Plan violet-prep + checklist); **upload** (`docs.upload.saving`) becomes a **determinate `NN%` + mini-bar** replacing the indeterminate `מעלה…` spinner (offline stays `ממתין להעלאה`). Amber kept a **time accent, not a ground** (a full amber field was tried and rejected off-budget). Extends 0078, relates 0052/0028/0082/0091/0092/0056/0016/0040 | Accepted |
| [0106](0106-maps-and-places-epic-scope-and-phasing.md) | Maps & Places epic — scope + phasing (a PM frame, design/architecture deferred): **one Map tab re-emphasized by mode** (Plan research + Trip near-me/today's-places, like Home/Day); the Google Places **picker bundled in as the foundational first task** (enriches every `Place` with `googlePlaceId`+coords, dedups, re-fixes the EventForm place-authoring hole 0051 left); **list-of-pins + Google Maps deep-links first, an embedded rendered map as a defined fast-follow**; filters are a **pure client-side derivation over the snapshot** (offline-safe) reading a place-usage index — day/type/maybes near-term, "by area" deferred to the map phase, Trip-mode-defaults-to-today as the mode pivot; **cost discipline** (the `Place` row _is_ the cache per 0048, dedup by `googlePlaceId`, session tokens; deep-links + list/filter are free); navigate-to-next returns ADR-0045's fourth Home tile + lands on the Map; OUT = member GPS sharing (0006), offline tiles, AI enrichment (vNext), cross-trip dedup. Six-phase order. The fast-follow embedded map is on the Maps **JS API** (not the Embed iframe), fully brand-styled (cloud styling + our own `AdvancedMarkerElement` pins, night/day on `data-theme`) with a **quiet base + loud semantic pins**, day-connectors on a free→paid spectrum (`Polyline` + whole-day deep-link free, Routes-API ETAs paid), macro per-day; a route ETA between hard anchors is the "when do we leave" answer. Refines 0004/0006/0038/0045/0048/0051 (relates 0028/0082/0087) | Accepted (scope + phasing) |
| [0107](0107-per-place-timezones-and-multi-zone-time.md) | Per-place timezones + the multi-zone time model (rides ADR-0106's picker for coordinates): the single `Trip.timezone` mis-renders zone-crossing transport (a TLV→NRT flight's departure shows in Tokyo time), but **storage is already zone-correct** (instants are absolute, 0018) and **now/next is already correct** (instant vs clock) — the bug is display/authoring/day-framing only. **Places carry a timezone** (free offline lat/lng→zone lookup, cached on the row, resolved in the Phase-1 picker). Separate three conflated roles — authoring default, **sticky** event display zone, and the live "now"; only the live "now" tracks position (so a fixed plan never appears to move). An event's zone resolves **place > itinerary segment > trip primary (destination) zone**, where **zone-crossing transport partitions the timeline into segments** (before the outbound = **origin/home** zone from the flight's `fromPlace`, after = destination); the **trip primary zone** (destination, what `Trip.timezone` demotes to) is distinct from the origin/home zone and is the Plan-mode framing + no-transport fallback. Position comes from the **itinerary, not device GPS** (GPS is confidently wrong at the origin airport on flight day); transport renders start/end in origin/destination zones and a zone-crosser gets **a transition entry per end filed under its own zone's day** (reusing ADR-0064's transition rows); "today"/day-strip roll at the current segment's midnight; the resolved zone is a visible, editable chip. `Trip.timezone` demoted, not deleted; a nullable `Event.displayTimezone` proposed for placeless stickiness (store-vs-derive is the open sub-question). No instant migration; the DST machinery (`zonedIso`) already exists + is zone-parameterized. `Place.timezone` folds into ADR-0106 Phase 1, the display/segment layer runs alongside Phases 2–3. Refines 0018/0032/0036/0037/0064/0048/0051/0084 (relates 0026/0070/0003/0106) | Proposed |
| [0108](0108-maps-and-places-backend-architecture-key-model-and-cost.md) | Maps & Places **backend/cost/library shape** (paper, answers ADR-0106's first open question; no feature code): a **two-key split** — a **browser key** (Maps JS API only, HTTP-referrer-locked to the single origin, Phase 6, blast-radius = cheap map loads) because the JS API embeds a key in the browser regardless, plus a **server key** (Places API New + Routes API) held by a **thin backend proxy** on the existing `places` module so the expensive SKUs (Place Details ~$20/1k Enterprise, Routes up to ~$15/1k) are never on a public key, the DB-side **dedup-before-spend** cache floor is enforceable, and paid calls sit behind `MembershipGuard` (only trip members spend quota; session tokens pass through) plus a **per-member·trip rate limit reusing the existing `@nestjs/throttler`** (keyed `userId:tripId`) and a **required Phase-0 budget alert + per-SKU daily quota cap**. Timezone library = **`geo-tz` server-side, resolved once at Place write, cached on the row** (accurate OSM-polygon lookup, free, offline, Node-only is fine on the server) — rejected `tz-lookup` (isomorphic/small but ~~10–30% border mismatch, defeats ADR-0107's point; coords only arrive online so backend resolution keeps offline intact). Caching = **the `Place` row _is_ the cache** (ADR-0048), _not_ the blob-cache tier (that's an immutable-ciphertext S3 mirror); adds `Place.timezone String?` + `@@unique([tripId, googlePlaceId])` (NULLs distinct), mirrored in `@waypoint/shared`; extends the `places` module (proxy routes + one `google-places.client.ts`, key name once in `env.ts`, writes via `ChangeService`, ownership via `trip-scope.util`). Confirmed current pricing (2026-07-23, not from memory: $200 credit retired March 2025 → per-SKU free tiers + Essentials/Pro/Enterprise field-mask tiers) + the Phase-6 envelope (map loads on the browser key; Routes through the proxy at the Basic field mask; free connectors ship before paid Routes; Routes not row-cached — traffic-sensitive). Refines 0106/0107/0048 (relates 0020/0031/0096/0055) | Accepted |
| [0109](0109-map-tab-design.md) | Map tab **design** (fills in ADR-0106's deferred visual/interaction shape + ratifies its open calls): the **list-first surface re-emphasized by mode** (Trip pre-selects today, Plan defaults to all) with per-row deep-links; **day scope via the existing shared header day strip** (`DayStrip`) reconciled — the map is a day-scoped surface like the Day view, so tapping a day focuses it **in place** (the strip's existing update-in-place rule; needs decoupling `setActiveDate` from the forced `days`-tab landing, ADR-0035 — flagged for FE-arch), and "all days" is a **map-local chip** since the app tracks one active date; the **filter chips (type · maybes) reusing the Index grammar** (0098/0100 — label+count pills, edge-fade mask, covering search, mode-tinted `--idx-accent`), pure offline-safe derivation, not a second copy (CLAUDE.md rule 8); **category-coloured pins** on "quiet base, loud pins" (0106 C / 0028 / 0105) — the 5-hue palette (0038 §2), a rounded **badge** in the list + a **teardrop (tip down)** on the map; commitment = the hard/soft grammar not colour; teal for location affordances, amber for time (route ETA + next-stop ring). Category comes from the referencing entity incl. **non-booking events**, chosen via an **explicit category selector** (§11 — amends 0038 §4: the icon no longer decides category, glyph-only everywhere). Cached Google **rating** pulled into Phase-1 enrichment (§9). Resolves 0106's open questions: **multi-facet = union + colour-by-most-committed** (which category colours the pin); **multi-day place = edge-loud, middle-ambient** (0054/0064); **geolocation = just-in-time** (0006, degrades if denied); **near-me = list re-sort + distance chips**, no spatial dot before Phase 6. Designs the **Phase-6 map↔list integration** (map pane + draggable list sheet + `רשימה/מפה` toggle, §10), 0107's **editable zone chip**, and **places+timezones in forms** (1 place / 2 places). Revises **0106 Decision C** (teal-on-pins → category colour) + **0038 §4** (icon→category). Two small model touches (`Place.rating`, explicit category field); rest presentation, FE-arch owns layout. Refines 0028/0087/0038/0098/0100/0054/0064/0006/0105 | Accepted (design) |
| [0110](0110-maps-and-places-frontend-architecture.md) | Maps & Places **frontend architecture** (paper, answers ADR-0106's two remaining FE-arch open questions; no feature code): **one shared search core, two shells** — a net-new hook `lib/usePlaceSearch.ts` owns the FE-minted session-token lifecycle, the mandatory pause-gated autocomplete debounce, the `alreadyInTrip` dedup match (a client-side derivation over the snapshot, not a proxy field), soft 429 handling, and the offline name-only fallback; two proxy calls extend `lib/api.ts` (`searchPlaces`/`resolvePlace`, `enrichPlaceId?` for enriching an existing Place-lite); `ui/primitives/PlacePicker` is the single-select shell and Phase-5 research a second shell over the same hook. **Hard boundary:** outbox = offline-capable writes (name-only Place-lite via the existing `CREATE_PLACE` verb), proxy = online-only enrichment (not outboxed); online resolve reuses the existing `place` sync registry (0094). **Place-usage derivation** → one `lib/place-usage.ts` built on the existing linked/unlinked resolver (`lib/places.ts`, not re-derived) feeding **both the filter chips and the pin colour**: union semantics + colour-by-most-committed (`hard>soft>idea`), multi-day edge-loud/middle-ambient (via shared `isMultiDay`/`isAmbient`), coordless flagging, a `CATEGORY_PIN_HUE` `Record<EventCategory,PinHue>` (9→5, uncategorised→leisure); filters reuse the `ChoiceGrid` pills + `SearchOverlay` + `--idx-accent` primitive, the counting is map-specific new logic in the same idiom (not a generalization of `index-bookings.ts` — flagged). **Per-event zone threading** through `lib/places.ts` (`eventDisplayZones → {start,end}` sticky resolver reading cached `Place.timezone` only; a `partitionZoneSegments` helper keyed off zone-crossing transport) + `lib/time.ts` (thread the resolved zone where `trip.timezone` is passed to `zonedIso`/`formatTime`); the zone chip is an addition to `WhenField`/`TimeField`. **Resolves ADR-0107 §7's store-vs-derive:** `Event.displayTimezone` is a **manual override, derived by default** (null → trust place>segment>primary so 'add the flight → times reorient' works; non-null → the user pinned it via the chip). **Context-aware `setActiveDate`** — a named `DAY_SCOPED_TABS` set (`days`, `map`) so `daySelectTarget` preserves a day-scoped tab instead of always landing on `days` (the Map focuses days in place like the Day view); `resolveBack` untouched; 'all days' is Map-screen-local state defaulting by mode, not a second URL source. Refines 0106/0107/0109/0035/0090 (relates 0048/0051/0094/0095/0038/0028/0108) | Accepted |
| [0111](0111-places-field-mask-tier-and-rating-deferral.md) | Places field mask stays at the **Pro tier**; `rating`/`userRatingsTotal` deferred off the Phase-1 pick (implementation-time cost decision, discharges ADR-0108 §3's "confirm the field→tier mapping at implementation" note). Confirmed against Google's **live** Place Details (New) field→tier list (2026-07-23): `id`/`formattedAddress`/`location` = Essentials, `displayName` = Pro, `rating`/`userRatingCount` = **Enterprise**; billed at the highest tier present. The set ADR-0109 §9 wanted (id/name/address/location **+ rating**) would bill every pick at Enterprise (~$20/1k, **1,000**/mo free); dropping the two rating fields bills at Pro (~~$17/1k, **5,000**/mo free) — 5× the headroom, and nothing renders a ★ until Phase 2/3. So the Phase-1 mask is `id,displayName,formattedAddress,location`; the `Place.rating`/`userRatingsTotal` columns still ship (nullable, unpopulated) so opting in later is a one-line mask edit, no migration. Refines 0108 §3 / 0109 §9 (relates 0106/0110) | Accepted |
| [0112](0112-place-in-trip-is-referenced-not-cached.md) | "In the trip" is a **reference derivation, not row-presence**; a picked-but-unsaved place is **cache-only**. The picker persists a `Place` on pick (the dedup/enrichment cache, ADR-0048) — but the "already in trip" chip keyed off mere row-presence, so picking a place then **cancelling** the form left it reading as in-trip forever. Fix: `referencedPlaceIds(events, bookings, maybeItems)` in `lib/places.ts` — a place is in the trip iff referenced by an `Event`/`Booking`(placeId/from/to)/`MaybeItem`; the picker's `alreadyInTrip` keys off that. A cancelled pick's row stays as cache (re-picking still dedups at zero Google spend) but is not in the trip. Derived, not a stored flag — no schema change; same concept the Map's place-usage (0110 §2) uses. Cross-trip global cache noted as a deferred scale optimization (still 0106-OUT). Refines 0110 §1 / 0048 / 0108 §3 | Accepted |
| [0113](0113-trip-destination-place-and-primary-timezone.md) | Trip **destination becomes a picked place** (Google Places, any granularity — city/region/**country**) that **sets the primary timezone** at creation (ADR-0107's demoted `Trip.timezone`, today a manual `UTC` default). Timezone is a **derived default, never a forced choice**: `geo-tz` on the picked point always yields one zone; single-zone places set it silently, a multi-zone country (US/Australia) shows it pre-filled + editable with a soft "spans multiple zones" note (no modal, no IANA list to decode) — safe to leave imperfect because per-event/place zones override it and it self-corrects as the itinerary fills in. **Origin stays derived** from the outbound flight (0107 §3/§5 upheld, not stored). Also owns **how you pick a zone**: one shared searchable `ZonePicker` over `Intl.supportedValuesOf('timeZone')` (the full IANA set) that **replaces trip settings' hardcoded 5-item `<select>`** and is the same control the per-event zone chip (0110 §3) uses — one primitive, three call sites (creation · settings · event chip). Foundational slice of the multi-zone workstream (build first). Needs a trip-agnostic search+geocode endpoint, a Trip migration + `createTripSchema`/shared mirror, an optional country→zones map, and a creation-flow picker. Refines 0032 / 0107 / 0039 (relates 0108/0110/0018/0038) — **Accepted (fully implemented, sessions 82–84)** |
| [0114](0114-elapsed-duration-ladder.md) | One **elapsed-time duration ladder** — a duration's rounding unit is chosen by elapsed length, **never** by how many calendar dates a span touches (fixes the reported 23:00→00:00 booking reading "יומיים" instead of "שעה"). `formatDuration(minutes, unit)` in `frontend/src/lib/duration.ts` expresses a length in the largest rung it fills, rounded to nearest: minutes → hours (H:MM) → days → weeks → months → years, boundaries as named constants. The two ADR-0084 per-kind overrides stand, applied by the caller: **nights** (lodging, calendar nights) and **hours** (transport, stays in hours past a day). The `auto` path reads real elapsed time from the two instants; the one calendar-count exception is a **date-only** multi-day span (no times to measure → inclusive days). Collapses the three drifted hour/minute copies (`booking-timing.hoursPhrase`, `WhenField` via `formatCountdown`, `TimePicker.durationPhrase`) onto the one helper. The "when is this" surfaces — relative-day + forward countdown (0085) — are a separate concern, deliberately not routed through this ladder. Frontend + docs only, no schema change. Fixes/standardizes 0084 (relates 0063/0085/0028) | Accepted |
| [0115](0115-plan-mode-place-research.md) | **Plan-mode place research** (Phase 5 of the Maps & Places epic; the design ADR-0109 deferred to its build session, plus the build). The Map tab's **one** search control gains a second half, and **the paid half is armed by intent**: the overlay opens free (filtering the trip's own places — pure derivation, offline-safe, unchanged) and in Plan mode also offers `חיפוש בגוגל`, because this is the first surface in the app that **spends money per keystroke** and filtering your own list must not be the same gesture as buying an Autocomplete session; once armed the behaviour is the in-form picker's verbatim (min-chars floor, pause-gated debounce, one session token, dedup-before-spend) with the `usePlaceSearch` core **untouched** — no second search path. A **result card says only what the relay returns** (neutral pin badge + primary name + secondary address + one `＋ אולי`; the name is a free `place_id` Google Maps deep-link so a candidate is vetted before we pay to resolve it) — which **drops `plan-mode-v1.html`'s `4.5★` (Enterprise-tier, deferred by ADR-0111), `1.2 ק״מ` (a prediction has no coordinates), and its category glyph (types aren't in the mask)**: a card renders the fields we hold, and a mockup drawn before the cost model is not a spec for what to fetch. **`＋ אולי` _is_ the pin** in a list-first world: one `pick` (enrich-or-link, zero spend on a dedup hit) then an **uncategorised** unconsumed `MaybeItem` referencing the place — the reference is what makes it in-trip (ADR-0112), so it appears on the Map list (`על המדף`) and the Plan shelf; the existing toast/undo covers it and an undone add keeps the cached row, so re-adding is free. A result already referenced is **stated, not re-addable** (`על המדף` / `כבר בטיול`, both free derivations). **Offline the Google half is absent, not disabled** (the near-me rule, ADR-0109 §7); a 429 stays a soft banner. **Plan mode only** — on-the-ground discovery is a different query shape and SKU, and the one surface where a stray paid call is least welcome (its own ADR if ever). One shared-state change: `verbs.addMaybe` takes an options bag and carries `placeId`. Refines 0109 §2/0110 §1/0111/0112/0038 (relates 0106/0108/0028/0078) | Accepted |
| [0116](0116-day-aware-shelf-and-idea-target-day.md) | **The shelf becomes day-aware**, and skip vs. park is finally two clear verbs. The shelf shipped as a flat trip-wide pool (both hosts inlined `maybeItems.filter(m => !m.consumed)` — no day, no order, no cap) and ADR-0115 just made ideas cheap to accumulate. An idea gains an **optional `targetDate`** (nullable, additive migration, mirrored in `@waypoint/shared`): **pencilled in, never a schedule** — no time, no slot, off the timeline, out of `remaining`, freely clearable back to "someday"; storing it beat ranking the shelf by a derived per-day "fit", which is a guess the user can't correct about a question that is human intention. The shelf renders **two groups** — `לְיום הזה` (that day's ideas + that day's skipped soft events) then `רעיונות` (dateless first, then ideas aimed at another day, **each naming its day** via `relativeDayLabel`, ADR-0085) — with a header only when a group has content, so an unused field changes nothing. One shared `lib/shelf.ts` derivation feeds both hosts, which is how **ADR-0027 §2's union finally renders in Plan mode** (the day's skipped soft events were invisible on the surface you rebuild from). **skip** stays "not happening" (status, keeps its date, restorable in place) and **park** stays "keep the idea, drop the slot" (becomes a `MaybeItem`) — merging them was rejected because it destroys the "was scheduled for 14:00 and we bailed" record the settle strip exists to resolve; park is now reachable in **both** modes (`EventCard` ⋯, soft-only, gated where delete is) and stopped dropping the event's **category**, carrying its **date** as the idea's target day. **Slotting:** the Trip-mode `ScheduleSheet` retires the last bespoke time-only control for `WhenField variant="day"` (ADR-0083) so an idea can be put on Thursday without navigating there, `minDate`/`maxDate` honouring ADR-0029's day-scope; and a shelf card can be **dragged onto a gap chip** in Plan mode (free time, no displacement — the builder's existing pointer-capture drag, slot on the element's `data-gap-*`), which makes the long-standing `גרור ליום` hint true. Dropping onto an occupied row stays out (a ripple decision, ADR-0041). Refines 0027/0025/0029/0038/0083/0085 (relates 0115/0041/0109) | Accepted |
| [0117](0117-map-place-outcome-states.md) | **The Map says what happened at a place** — three states out of data that already existed. The ahead/behind partition was computed **purely from the clock** (verified: neither `place-usage.ts` nor `Map.tsx` referenced `event.status` at all), so a place you **skipped** sank under `כבר היינו` — claiming a visit that never happened — and a place marked done early stayed "ahead of you", while the most common case (a day passed, nobody settled it) had no name. `DayUsage` gains **`outcome`** (`done` beats `skipped` across a day's references) and **`settled`** (every reference settled) — stored facts, not clock reads, so the derivation stays clock-free and ADR-0027 §1's "status is only ever human-written" holds. `isDayUsagePast` treats a **settled** day as behind you whatever the clock says (a human outranks the clock). Rows carry `היינו`/`דילגנו` in the day view's existing words and the **`--ok`/`--miss` status hues** (ADR-0028 — amber stays on time, teal on location, no new accent); a skipped-only row goes **quiet**, reusing the ambient treatment. The block header becomes the neutral **`מה שמאחורינו`** (it holds three outcomes, only one a visit), and `מה שלפנינו` appears **only when** a behind block exists. **No schema change**, and the `pin.commitment` weights + ADR-0109 §4's colour tiebreak are deliberately untouched (re-weighting by outcome would silently rewrite a colour rule). Rejected: a stored `visited` flag on `Place` (a second source of truth beside `EVENT_STATUS.DONE`), two states only, and auto-settling passed events. Refines 0109/0027/0110 §2 (relates 0011/0028/0018/0043) | Accepted |
| [0118](0118-numbers-in-hebrew-bidi.md) | **A number and its Hebrew unit: the LTR island is the number, never the token.** Reported twice on one day — the Map distance chip read `ק״מ 9` and the zone-shift pill put the hours unit before the number. Neither string was built backwards; both elements were wrapped in **`dir="ltr"`**, which sets the base direction of the _whole_ element, so a token carrying a Hebrew unit laid out left-to-right and the Hebrew reader met the unit first (verified in Chromium by box positions). Two mechanisms in one new `frontend/src/lib/bidi.ts`: **`ltrIsolate(run)`** (`U+2066…U+2069`) for a Latin/numeric run — needed independently, because a signed `−3` in the RTL flow renders its minus on the wrong side of the digits — and **`measure(value, unit)`**, the single builder for every number-plus-unit token (value isolated, unit outside in the RTL flow). Strings, not a component, so the same rule reaches `title`/`aria-label` copy. Element-side, **`dir="ltr"` → `dir="auto"` across all 75 JSX sites** (never worse: identical for Latin-led or strong-character-free content, different only for the Hebrew-led case that was broken; it also deleted the Map's hand-rolled "force LTR unless the content is the Hebrew placeholder" branch), the two reported tokens carry **no `dir`** at all, and `<input>` keeps `dir="ltr"` (Latin-by-construction values; `dir` also drives native date/time control layout). The guard is a **lint rule** failing CI on `dir="ltr"` on any non-`input` element, naming the fix — a static rule can't inspect dynamic children, so it bans making content direction the element's business. Two unreported instances of the same defect were fixed with them (`TimePicker`'s `עד 21:30`, the stale-distance branch). Sharpens 0028's RTL/mono wording (relates 0109/0107/0096/0114) | Accepted |
| [0119](0119-map-maybes-facet-is-the-shelf.md) | **The Map's `אולי` facet is the shelf.** Three reported bugs on one chip, each with its own cause. An idea pencilled in for a day had **no day facet** (`buildPlaceUsageIndex` passed `days: []` for every `MaybeItem`), so "maybe today" was filtered out by the day scope Trip mode opens on — the consequence ADR-0116 §1 promised and never built; it now contributes one **clock-free** day (no `at`/`until`/`sortOrder`/`eventId`, so it ranks as untimed and never reaches the timeline), named on the row in a **neutral** tag because amber is time & commitment (ADR-0028). The facet itself read `isMaybe` — an unconsumed `MaybeItem` — so a **skipped soft event**, the shelf's other half (ADR-0027 §2), showed under its type chip and never under `אולי`; `PlaceUsage` gains `isParked` and the facet reads `isOnShelf = isMaybe \|\| isParked` (soft only, the same rule `lib/shelf.ts` groups by), while `isMaybe` keeps its narrow meaning for research's `על המדף` badge. And each facet's count now reflects the other — the `אולי` count follows the picked type, the type chips count only shelf places while the toggle is on — so a chip can no longer promise rows the list won't render. One derivation (ADR-0110 §2), no schema change. Relates 0109/0110/0116/0027/0117 | Accepted |
| [0120](0120-filter-reveal-is-shared-infrastructure.md) | **Filtering a list is a reveal, everywhere — the stagger becomes shared infrastructure.** ADR-0098 §4's motion pass had two halves; only the collapse was generalized at build time (`ui/primitives/Collapsible`). The **filter reveal** stayed a one-off on the Index bookings screen: a `BookingRow`-typed `visibleRows`, a `.index .idx-row` rule capped at `max-height: 140px`, and the wrapper markup copy-pasted at each of the screen's three lists. So the Map tab — built to reuse the Index's filter grammar (ADR-0109 §2), same chips, same `SearchOverlay`, same accent — `.filter()`ed rows out of the array and jumped. Now: `lib/filter-reveal.ts` (`revealRows` + `countVisible`/`visibleItems`, generic, the caller keeps the predicate) and `ui/primitives/RevealList`/`RevealRow` (+ unscoped `.wp-reveal`, motion-ramp durations, `renderBefore` for group headers outside the collapsing wrapper). Two mechanism changes the one-off didn't have: a hidden row is **`inert`** (it stays mounted to animate, so it was focusable and read aloud), and the collapse is a **`0fr`/`1fr` grid track** instead of a per-surface `max-height` cap (with a layout-neutral clip bleed so a row's shadow/focus ring/next-stop ring isn't sheared). **Amended session-130** (owner call, same day): _every_ list change animates — `כל הימים` and the strip's day became predicates over the whole trip rather than a different array, and a pure **re-order** (`קרוב עכשיו`) slides its rows via FLIP (`lib/useFlipRows.ts`, WAAPI so it never fights the reveal's own transform/delay), wired inside `RevealList` so no call site opts in; the verification also turned up `useGeolocation`'s un-re-armed liveness ref, which had near-me stuck on `מאתר…` in dev. Consequence: a filtered-out row is now hidden-in-DOM rather than absent, and the next filterable list (documents search, the shelf's filter row, an outcome facet) is a two-line adoption. Refines 0098/0101/0109, relates 0096 | Accepted |
| [0121](0121-embedded-map-phase-6-design.md) | **The Phase-6 embedded map**, designed after the API/pricing reconfirmation ADR-0109 and the backlog both demanded first. **The reconfirmation:** ADR-0106 §A–F and ADR-0108 §1 held (the JS-API path, the split key, cloud styling, `AdvancedMarkerElement`, free connectors before paid routes); two details moved — a **`mapId` is now mandatory** for advanced markers rather than merely enabling them (a new human Phase-0 step: create a Map ID + cloud style), and **Compute Routes Essentials caps at 10 intermediate waypoints**, not the ~25 ADR-0106 §D assumed. **The binding is `@vis.gl/react-google-maps`**, a real dependency in a four-dep frontend, justified where a hand-roll actually goes wrong: the loader is a singleton problem, React-19 StrictMode is the bug class session 130 just paid for, and `screens/Map.tsx` re-renders **every second** — this is the one surface where a lifecycle mistake is _billed_. **Dynamic Maps bills per map instantiation**, so one instance per tab visit is accepted on arithmetic (~100 loads/day for a 5-person trip against 10,000/month free) while re-creating on the view toggle, a filter, a sheet drag or the clock tick is forbidden, and no map is created while the user is on the list half; a global map singleton above the router was rejected as clever-that-leaks for a saving we don't need. **The shell** is ADR-0109 §10 made concrete: a map pane over a **three-height draggable list sheet** (peek/half/full) with the `רשימה·מפה` toggle as a shortcut to the two extremes of the same axis, and the sheet **always peeks** because the list is the only view that works offline and the only one that can hold a coordless place. It is **view state, not a back layer** — a pane of the tab, not an overlay over it, so no `Modal`/`useOverlay` and no back registration (back leaves the tab at any height). **Pins** are `AdvancedMarker`s carrying **our** markup over the **same** `place-usage.ts` derivation the list reads — `PinElement` cannot express the dashed-idea / desaturated-ambient commitment grammar, so badge and teardrop share the `--cat-*` tokens by construction; coord-bearing places only (which retires ADR-0109 §3's hollow coordless pin as unbuildable), and §6's single amber next-stop ring finally moves onto a pin as the session-104 amendment promised. **The camera answers the controls the way the list does** (ADR-0120): an animated fit to the filtered set on every set change, while a manual pan wins until the next scope change; a row tap focuses its pin, which is where Phase 2's interim `מפה` deep-link finally ends. **Day connectors are dashed, neutral, and day-scope only** — a straight segment is the day's order, not its route, and one day means no per-day palette (making the tab's scope chip ADR-0106 §E's "day toggle"), leaving solid+amber unspent for a real Routes polyline. **Offline the map is absent, not disabled** (the near-me/research rule, third time). `disableDefaultUI` + `gestureHandling: 'greedy'` + our own re-centre control, Google's attribution kept per ToS; two cloud styles with the night one inert until dark mode ships. Out of scope: paid Routes, an area chip (pan/zoom _is_ the area filter), clustering, offline tiles, a visible dark map. Mockup: `mockups/map-embedded-v1.html`. **Built session 133** — see the ADR's build log for the ten places the build refined it, the sharpest being that `מה נשאר` could not reach a **ghost** until the settled check fell back off the scoped day, and that "View on Google Maps" is retired **all the way** (`mapsPlaceUrl` and its two wrappers deleted rather than left as dead exports). The **render remains unverified by the suite**, as §13 said it would: a human visual pass on a machine with the browser key is the remaining step, and it is where the two questions left open by decision get answered. |
| [0122](0122-map-split-controls-over-the-canvas.md) | **The map split earns its screen: the controls leave the layout.** The map panel's second-pass Phase 2 — designed session 140, **built session 141** (three reports: the map and the list are both too small, the drag between the sheet's heights is unpleasant, the geolocation pre-prompt is in the wrong place) — plus the owner's call, mid-session, that the filter chrome is **cluttered** and may render **over** the map. Everything here was measured in Chromium against the real layout tree (`AppShell` + `BODY_FULLBLEED` + `.map-split`, `.nav` included), because Phase 2 _is_ a layout-tree change and session 131's mockup silently supplied a flex column the app did not have. **The measurement is the finding: 370 of 844 phone pixels are spent before either half of the split gets one** — 207 header, 69 nav, and 94 for this tab's two fixed rows — which is why `half: 0.56` was blamed for a problem it did not cause (it splits what is left almost exactly in half; what is small is what is left), and why `peek: 116` showed **0.8 of a row** at the stop whose job is "a row or two". **The decision: `.map-filter-row` + `.map-sortstrip` stop being the split's fixed header and become ONE row floating over the canvas** (measured: the pane goes 208→250 at `half` and 358→420 at `peek`, the list 2.6→3.2 rows, and every stop gains because the split becomes the whole body). Merging the two rows _inside_ the layout — the phasing note's first candidate and this session's first draft — recovers 28px, ~6% per half, and was rejected on that arithmetic; collapsing the header per stop was rejected because it buys one row out of six at the only stop where it is defensible and pays by relayouting the canvas on a gesture ADR-0121 §5 made relayout-free. **Pins are kept out from under the row by the CAMERA, not the layout**: §7's fit already insets by a pin's own height, and that inset now derives from the same constant that writes `--map-controls-h` — with two honest limits stated (a manual pan can still put a pin there, and `fitPaddingFor` drops padding claiming half an axis, so at `half` on a small phone the inset _will_ be dropped). **Decluttered to three controls at rest** — scope · one `סינון` control · search — with the facets opening in place on one tap (ADR-0100 §3's shape — and the citation carries its own correction, since ADR-0101 superseded it on the Index because search-in-place left no room for the keyboard or the results, neither of which a facet strip has, which is precisely why it belongs here), the categories as **glyph + count** chips because the glyph is already the category's whole vocabulary (ADR-0038; all six now fit one 390px row where the worded pills fit two and a half), and the collapsed control stating _which_ facets are on with **no count** (a fourth number is a fourth thing to keep coupled, ADR-0119). **`קרוב עכשיו` moves to the sheet's top row**: scope belongs to the tab, filters to the split, **sort to the list** — the distinction session 138 already made in state. **The gesture:** the whole `.wp-snapsheet-top` becomes the target (390×51 from 76×16, ADR-0017's floor), which needs a slop threshold, window-bound move listeners and pointer capture taken at **drag start** — the mockup got the last two wrong first, and capturing at `pointerdown` silently retargets the following `click` to the capturing element, killing every tap in the region; `nearestStop` gains a velocity term so a flick commits in its direction of travel; the handle becomes a real ARIA splitter with arrow keys (today it is focusable and inert, so `half` is unreachable without a pointer); and the toggle's fill becomes a thumb that sits **between** the labels at `half`, drawing the third stop the control always had. **The list-sliver peek is retired, and a tapped pin surfaces its place as a card on the canvas** — the owner's second call, and the one place a first draft of this ADR was overturned rather than refined. Asked what the sliver adds when the map is maxed, the first answer was "it is where a tapped pin speaks"; the arithmetic says that row earns its 97px only **while something is selected**, so the map extreme becomes the sheet's own top row and **nothing** of the list (517px of map against 358 shipped, and 420 with a one-row peek), the bottom stop is renamed `map` after the word the toggle already uses, and the tapped place arrives as a **card over the canvas with nothing moving** — the pane's box is unchanged, so the camera does not shift. The card **is the `.place` row in a second host**, not a second vocabulary (one grammar, two hosts, the way the pin is the badge in a second form factor), it carries the same way-in block so acting on a reference never needs the sheet to move, it renders **only where the sheet cannot show the row** so it never doubles it, it clears Google's attribution by the attribution's own height (ToS), and it **generalises the shipped `.map-ghostrow`** — one mechanism fewer, not one more. That **closes ADR-0121 §8's open info-window question on the canvas and as the row**, and **revises session 136's pin-tap raise** (right for a 65px viewport) while keeping its scroll where the list is showing, under the rule that explains both directions: **a tap never takes away the surface it was made on**, so a **row** tap normalises to `half`. A canvas tap clears the selection; nothing registers with the back stack. **One motion idiom covers the three objects that come and go** — the sort chip, the place card and the pre-prompt all rise and fade at `--t-quick` — with the chip hidden by `visibility` rather than `display` so both directions animate while it still leaves the tab order and the accessibility tree (a control you cannot see must not be tappable), and with **stop-driven hiding animated where capability-driven absence stays unmounted** (offline the chip is gone, not faded). **The pre-prompt becomes canvas furniture** — absolutely positioned inside `.map-split`, so it costs the split no height and the camera does not move for it, a sibling of `.map-pane` and never a wrapper (a remount is a billed load), still inline and not an overlay (ADR-0109 session-105), not at the pane's bottom (Google's attribution, ToS) — while the **refusal notice stays in the list**, because that one explains the list's order. `full` becomes a third `SnapStop` variant (container minus the row) so the sheet cannot cover the controls of the list it is showing. Both standing constraints survive verbatim: `--sheet-h` from the **snapped** stop, and nothing re-creates the `google.maps.Map`. **The numbers are a starting point, not a calibration, and the ADR says so**: `half`'s fraction, `peek`'s sliver and the flick threshold are legibility and feel, which need a phone — they join `MAP_ZOOM`/`MAP_REFIT_FILL_SHARE` in Phase 3's tuning cluster. The app chrome's 276px is **named and deliberately not touched** (cross-tab, and the day strip is _why_ the Map is day-scoped) — agreed with the owner as its own design session, backlogged with what it must decide, what it must not break, and how to measure it. Mockup: `mockups/map-split-v2.html`, which reproduces the shell and **prints its own measurements**. **Built session 141**, with §1–§9 unreversed — see the build log for the refinements (the strip height reaching CSS under the primitive's own `--snap-top-h`; `nearestStop`'s flick branch kept separate so zero velocity is byte-for-byte the shipped behaviour, plus the epsilon a flick released exactly on a stop needs; the click-swallow armed only on `pointerup`, since a cancelled gesture dispatches no click) and the **one place it read against the letter**: §7's card-driven **bottom** camera inset is not built, because carrying a card that comes and goes on a tap means a `MapPane` prop that flips on a tap — the one thing §9 forbids — so Phase 3, which owns the camera, gets it. The **render was not seen**: the device pass and its numbers are unchanged and unspent. |
| [0123](0123-map-pin-size-is-a-share-of-the-canvas.md) | **A map pin is a share of the canvas it sits on.** Reported from the shipped tab on a screenshot of the map extreme — "the pins are much smaller than they could've been given the size of the pane… though when the map is sharing the screen with the list perhaps it's a different story". Both halves are right, and the second is why a flat bump is the wrong fix. `.map-pin` shipped at a fixed **28×34px** (ADR-0121 §6, ported from a mockup that had no three-stop axis), and ADR-0122 then made the stops move the canvas by more than **2×** — measured against the real layout tree: **~545px of pane at `map` against ~263px at `half`**, so 34px was 6.2% of one canvas and 12.9% of the other, tuned for the smaller. **The decision: one clamp on one parameter** — `clamp(34px, 8% of the pane's height, 46px)`, resolved **in CSS** against `container-type: size` on `.map-pane`, so the **floor is the size that shipped** (`half` is byte-for-byte unchanged and no re-review of the shared-screen case is owed), the map extreme gains **28%** (43.6px), and the cap (46px) is where a marker stops reading as a _point_. Keying on the **canvas** rather than the stop buys a second axis for free: a tablet's `half` and a phone's `map` are one rule, and a fourth stop would be too — which is why a `data-view` size table was rejected, since it needs a second breakpoint mechanism bolted beside it. CSS rather than state because `screens/Map.tsx` re-renders **every second** (a `ResizeObserver` into state is a layout read on the clock) and because a `MapPane` prop that changes on a gesture is what ADR-0122 §9 forbids; `--sheet-h` already comes from the **snapped** stop, so the browser re-resolves `cqh` once per stop change and the drag stays relayout-free. **Every dimension is a fraction of one `--pin-u`** — badge, glyph, number, amber rings, selection outline, tag — the fractions being the shipped geometry as ratios, so the ghost tier's "smaller" and the me-dot become **ratios** (0.72 / 0.41) instead of fixed boxes that would have gone on shrinking _relatively_ as the canvas grew the others, until the ladder's bottom rung read as dirt rather than as a place you are standing next to. **The resize is smooth**: `--pin-u` is registered with `@property` as a `<length>`, so **one** transition eases the whole pin over the sheet's own 280ms curve and the pins finish growing exactly as the sheet finishes sliding — beating a dozen per-property declarations that can fall out of step. **The camera's clearance is derived, not constant**: `MAP_FIT_PADDING` becomes `mapFitPadding(canvasHeightPx)`, its top being the controls row plus `pinHeightFor × (1 + TAG_RISE)` — the pin plus the amber tag above it — since a flat 64px can only be right for one stop (69px is needed at the map extreme; 54px suffices at `half`, on the one axis where `fitPaddingFor` already drops the padding). **Zoom and density are rejected as parameters**, both because they resize pins under a moving finger — so a **pinch does not resize a pin**, by construction, and its one honest cost (a fixed marker covers more ground as you zoom out) is exactly what ADR-0121 §6's **dot tier** was decided for and never built, now backlogged against Phase 3 because it is a change of _tier_ at a threshold, not of size continuously. **`transform: scale()` was the attractive alternative and is rejected on a CSS fact**: `scale()` needs a unitless factor and CSS cannot divide a `cqh` length by a px one, leaving only the `tan(atan2(…))` trick — cleverness that reads as a bug. Animating the pane's own `bottom` (so `cqh` eases for free) is rejected too: it resizes a live `google.maps.Map` every frame for 280ms, reintroducing the relayout ADR-0121 §5 removed. **Built session 142**, and the build's whole log is _verification rather than reasoning_ — a Playwright harness against the preinstalled Chromium confirmed that `cqh` reaches an advanced marker at all (the shipped pin CSS was no evidence either way, since every token it reads is on `:root`), that a `cqh`-derived value survives `@property` registration, that it interpolates as a real curve (36.1 → 43.6px across 280ms), that a pin **added by a filter change does not pop in** (which would have bounced the canvas on every chip tap), and that reduced motion **snaps** — that last check a false negative on its first run, because the harness had not copied `App.css`'s global reset and so measured the transition it was meant to suppress. **The three numbers want a phone** (floor, cap, share join `MAP_ZOOM`/`MAP_REFIT_FILL_SHARE`/`half`'s fraction in Phase 3's cluster — one question: how close is close enough to read a place in context), and a **1:1 preview** of every rung with a live stop toggle was produced for that pass rather than described. Amends 0121 §6/§7 (relates 0122/0017/0028/0098) | Accepted **Recalibrated on a phone the same day (session 143), shape intact:** the owner reported it still too small, and the rule was working as specified — `CANVAS_SHARE = 0.08` and `MAX_H = 46` had been set against ADR-0122's measured **390×844 budget** (a 545px map-stop canvas), which is a mockup baseline and not a device. The real viewport is shorter (**~501px** of canvas), so the pins grew 18% where the arithmetic promised 28% and never reached the cap. Derived by measuring, not eyeballing: the lodging pin's badge, segmented by its unique `--cat-lodging` violet across the two reported screenshots, went **67 → 79 device px**, and that 1.18 ratio run back through the clamp gives both the pin and the canvas with no DPR assumption. Now **0.11 / 56px** (~55px there, 1.6× the original), `MIN_H` and `half` untouched. The consequence is stated in `constants.ts` and the ADR rather than left to be found: the growth band is 309→509px, so a phone's map extreme sits **at the cap**, making `MAX_H` the knob for further re-tuning and leaving the share to protect `half` (which has margin — a ~243px canvas against a floor holding under 309px). Tests now carry **two** baselines, device included. Still open is a **case, not a number**: a dense all-days day, since past ~56px a tip gets vaguer and coincident pins overlap sooner. |
| [0124](0124-map-filters-scope-facets-and-what-is-left.md) | **The Map's filters, stated once — and `מה נשאר` means somewhere you can still go.** Reported twice in one session: "I'm not sure what stays at `מה נשאר` and what not… I'm not sure how compound filtering works", then "I see past events in `מה נשאר` and I really don't understand what's the usage here". Neither is a misreading. **The answer was spread across four ADRs** (scope in 0110 §4, the type chips and `אולי` in 0109 §2 as widened by 0119, `מה נשאר` in 0117-deferred-then-0121 §9, the count coupling in 0119 §3) — each right about its own piece, none saying what the tab does. **And the toggle was defined against something that rarely happens:** 0121 §9 made it "hide everything `settled`", but settling is a **manual tap** that 0027 §1 and 0018 both refuse to automate, so on a real trip almost nothing is settled, the filter hid almost nothing, and §9's own payoff ("with the settled pins gone the remaining cluster is legible") never arrived. **The decision: two things close a place, not one — a human closing it OR the clock**, which is `isDayUsagePast`, already both and already in the right order (`settled` is read **before** the clock, so a stop marked `היינו` for tonight goes now rather than at 20:00). So `isPlaceLeft` reads `placeBlock` rather than restating it, and the rule is one sentence: **`מה נשאר` hides exactly what the list files under `מה שמאחורינו`** — a filter and a block header become structurally unable to disagree. Kept deliberately: **no day at all → always left** (that is `ללא יום`, not behind you — the same rule, not an exception), **all its days rather than any**, and **a place with nothing in scope falls back to all of them**, which is what applies the toggle to the canvas's ghost tier. The chip's gate moves with the predicate (`hasBehind`, not `hasSettled`): the old gate was the old rule's blind spot, never appearing on a trip where nobody taps `היינו` though a morning was behind you it would have cleared. **0117 is untouched** — the three outcome states, tags and quiet skipped row all stay; what changes is that a map _filter_ stops being the surface for the unsettled past, which is the **settle strip's** job (0043). **A third defect fell out of writing the model down:** all-days read every place off `days[0]`, so one past day classified it however alive the trip still was with it — the hotel you sleep in **tonight** sat under `מה שמאחורינו` with a desaturated pin from its second night on (session 137 took that fade off the _ambient_ tier, but all-days never reached that tier, so `PIN_TIER.behind` re-applied it through `skipped`, harder than the paint removed), and a café visited Tuesday and booked again Thursday read as done — while `מה נשאר`, which asks about **all** a place's days, correctly kept both: two answers to one question on one screen. `placeDay` now takes a context and resolves **the day the place is live on** (earliest not-behind-you, its LATEST once they all are, since the behind block reads newest-first); **without a clock it is still `days[0]`**, and `buildPinOrderIndex` passes none, so 0121 §6's "a tick cannot renumber a pin" survives intact. All-days a stay in progress then names its next **edge** (the check-out), its two wrong answers having been the check-in it already passed and, once the live day resolved, nothing at all. **The compound cases are written down**: facets intersect, counts stay coupled (0119 §3), `אולי + מה נשאר` = ideas still open (a parked skipped-soft event is on the shelf _and_ closed — restoring is the shelf's affordance, not this filter's), and a type chip that empties falls back to `הכל`, which is why the **toggles** and the **day scope** are the only paths that can empty the list. **An empty list now says which of its three causes it is** and hands back the step out (0078): an empty trip, an empty **day** (offers all-days — the common path, since facets persist across a day change and the old copy blamed a control you never touched), or an over-narrow filter (names the facets it holds, since the strip may be closed over them, and offers to clear them — facets only; a scope is not a facet). A **derived chip that disappears while its filter is on** now clears that filter, the guard the type chip already had. Also fixed in the same pass: a surfaced **ghost** row's way in — `forceDay` reached the row's day and outcome but not `refEntriesFor`, so the one row whose tap is the only way to learn what it is showed an empty way-in block. Amends 0121 §9 and 0109 §1's session-110 amendment (relates 0117/0119/0027/0106 §4/0043) | Accepted |
| [0125](0125-map-canvas-terrain-vocabulary.md) | **The map canvas earns a terrain vocabulary.** Reported off the shipped style on a real phone: "the map style looks very lifeless to me and not really on the design language" — and the sea had already been made bluer **by hand in the Cloud Console**, a tweak the repo files never carried. The README had predicted "one adjustment round, most likely water contrast and the park fill"; measured, it was structural, not two numbers. **Every colour sat in hue 210–218, and base/landCover/building/park _and water_ all sat inside L\* 88.2–92.6** — water **4.4 L\*** off the land, park carrying a chroma of ~5 (a grey with a rumour of green), roads the only thing with real separation, so the map read as a road diagram on an undifferentiated wash. Night was worse: land↔water **2.6 L\***. **The decision, in three rules that generate the palette:** (1) **quiet means low chroma, not grey** — land goes warm (~43°) against cool water (~206°), both under 35% saturation, so temperature separates ground from sea and **nothing is added to the colour budget**; the base is one step off `--paper`, and `--screen` stops being the canvas because the adjacency that reasoning assumed does not exist (the map is full-bleed under a sheet with dark chrome above it). (2) **A real value range** — land↔water 4.4 → **14.3 L\***. (3) **Built vs natural splits by _chroma_, not lightness** — `urbanArea` 3.0 against `forest` 13.6, at nearly equal lightness, so city and countryside are told apart by saturation while lightness stays free for the figure-ground stack. **The canvas then earns a terrain vocabulary** (`landCover.forest`/`shrub`/`crops`/`dryCrops`/`tundra`/`ice`/`sand`, plus beach, airport, businessCorridor, trail, pedestrianMall footprints) on the owner's second report — _"this is a travel map, we should differentiate cities/nature, mountains/glaciers/lakes"_ — after the Apennines behind Genoa rendered as a flat cream field. **There is no relief shading to be had**: CBMS has no hillshade or landform feature, so mountains are drawn as what covers them and **land-cover banding _is_ the relief**, which is why a "terrain toggle" would not have answered it. **Park and nature reserve split by role, for pin legibility rather than cartography** — a `--cat-leisure` pin on the loud green measured **1.29:1**, so `park` becomes the quiet green (pins land there) and `natureReserve` the strong one (they don't). **Then the sights came back on**, on the third report — _"it doesn't pop up landmarks, tourist attractions etc"_ — reversing 0121 §11's root suppression for `landmark` / `entertainment` (attraction, museum, historic, arts, themePark) / `zoo` / `peak` / `natureReserve`, while **all commerce stays off** and `casino`/`cinema` are switched back off by name; the **parent** carries the treatment so a type Google adds later inherits it. The tension that creates is resolved by the same chroma rule: **Google's sights are achromatic** (a light neutral pin, chroma **5.0**) and **ours are chromatic** (27.8–51.8), so the canvas reads as _grey pin = exists, coloured pin = on your trip_. One code knock-on, where a draft of the ADR was **overturned by the owner**: Google's icons were already clickable, and that tap arrives as a map click carrying a **`placeId`**, so `MapPane` clears the selection on it. The draft exempted the POI tap to avoid "clearing the selection behind Google's card"; the owner's _"they might go one over another and become a mess"_ is correct, since 0122 §7 puts our selected place on the **same canvas** as Google's place card, so the exemption **stacks two cards** — and replacing a selection when you tap something else is the platform idiom anyway. The handler is therefore unchanged, with the reasoning in the code and pinned by a test, because the exemption reads like an obvious fix and is the bug. **Two rejections reversed and named so they are not re-applied:** the "no blue sea" paragraph (which conflated _blue_ with _saturated_ — the sea is a 33% slate at L\* 79, an order of magnitude off `--me` and a different hue family from `--teal`) and "POI labels off at the root" (a map of Paris with no Eiffel Tower is not quiet, it is uninformative). `monochrome: true` was rejected as "the disease, not the treatment". **`natural.water.lake`/`.river` cannot be coloured at all** — label-only in the schema — so a mountain lake and the coast are the same blue at any price. **The schema is now vendored** (`design/map-styles/cbms-json-schema.json`): `developers.google.com` is unreachable from agent/sandbox sessions (403 at the egress proxy), which is exactly how a first draft shipped an invalid id (`landCover.iceAndSnow`; the real one is `landCover.ice`) and two ids styled with properties they do not have. Both files validate. **The ground was judged on a device across two rounds; the terrain and sights layers have not been seen** — expect one pass on `forest` strength, on how loud the grey sight pins read at city zoom, and on whether `peak` earns its pin. Amends 0121 §11 and 0106 §C (relates 0028/0038 §2/0109 §3/0123) | Accepted |
| [0126](0126-map-canvas-chrome-two-camera-controls-and-an-area-sort.md) | **The canvas's own chrome, restated: two camera controls, and `באזור` becomes a sort.** Two third-pass reports on one surface, both settled by the owner before the session and both needing a layout answer it could not give from the code. **#19:** `recentre` is `if (me) focus(me); else reframe(points)` — one tap that centres you or re-frames the filtered set depending on a permission you cannot see. It splits: **locate becomes locate-only** and routes to the **existing reason-first pre-prompt** when it has none (so ADR-0121 §12's invariant survives — the card is still the only thing allowed to ask — and ADR-0122 §2's gap closes, since at the `map` stop the near-me chip is absent and nothing on the canvas could ask at all), while **"frame the filtered set" becomes its own control**, absent when there is nothing to frame. **#23:** the area is a **SORT, not a filter** — a tap raises the sheet and orders the in-view places first, hiding nothing; a sibling of session 138's `sortByDistance`, so the list keeps one order and the two intents are exclusive, with two `.map-grouphead` headers because "in view" is not legible per row, and bounds snapshotted **at the tap** since raising the sheet fires a fresh `idle`. **ADR-0106 §4 stays unamended: pan/zoom remains the only area _filter_**, and the rejected viewport-scoped list is recorded with its reason (an area predicate is the only facet whose predicate moves without the user touching the list, so it either churns under a panning finger or freezes out of sync). **Placement, decided by measurement against the real tree:** one **horizontal** furniture band under the controls row, because the scarce axis here is height — a vertical stack, the placement every desktop map uses, leaves **−3px** of clear canvas at 360×640 `half` and overlaps Google's attribution. **44×44 is met by geometry**, which also fixes two shipped violations nobody reported (`.map-recenter` 38×38, `.map-areacount` 64×26). **The readout becomes a control without ceasing to be one:** the live region **wraps** a `<button>` (`StatusBanner`'s own shape) rather than one node holding both roles, so the count exists once in the DOM and `title` carries the action while the visible text stays the accessible name. **And the prerequisite:** `areaCount` counts **ghosts**, which the sheet cannot render — the count **stays spatial** and the **list** states the shortfall in session 144's grammar with `כל הימים` beside it, because coupling the count to the list would show seven pins over a readout saying four. One rule falls out covering three cases: a canvas control whose answer lives in the list normalises the sheet to `half`. Design only; Phase 3 builds the camera side. Mockup `map-chrome-v1.html`. Amends 0121 §12 + 0122 §1/§2 (relates 0017, 0106 §4, 0109 §6-7, 0119 §3, 0122 §9) | Accepted |
| [0127](0127-map-camera-answers-the-tap.md) | **The camera answers the tap: one readable zoom, a stateless step-in, and an arrival that owns its frame.** Four Phase-3 reports, three of them the same question from different directions — selection panned but never zoomed, the single-pin zoom landed too close, and locate did not zoom at all — so they name **one** constant: `MAP_ZOOM.PLACE`, "how close is close enough to read a place in context", read by the lone-pin centre, the selection and locate alike, where before the tab landed at a different zoom depending on how you got there. **Focus zooms in one direction only:** ADR-0121 §7's "focus never zooms" protected the context you were reading, and that protection is entirely about not pulling BACK — so **zoom-to-at-least** (always pan, zoom _in_ below `PLACE`, never out) keeps §7's argument and drops only the half doing no work. A zoom change **jumps** rather than animating, because the distance was unreadable anyway. `MAX_FIT` stays separate and one step tighter: a fit knows the set's extent, a centre is a guess. **#20's repeat-tap step-in is stateless** — one level in from wherever the map IS, capped at `STEP_IN_MAX` — so a pinch between taps cannot desynchronise it and no tap count exists to drift (ADR-0122 §9). **And the arrival defect is fixed by ownership, not by another guard:** arriving from `מפה` landed on the day's frame because a pan and a fit both ran and the fit won; now an arrival focus **IS** the frame, claimed on the render that brings it, held until spent, and winning from **either** side of the race — so the ordering is irrelevant rather than out-timed, which is what the backlog line asked for after two previous fixes each added a guard. **The three zoom values are derived defaults (`PLACE` 14, `MAX_FIT` 15, both one step out from what shipped) and the device pass owns them**, ADR-0122's posture verbatim; they join `MAP_REFIT_FILL_SHARE` and `MAP_PIN` in one five-number cluster. Both zoom decisions are pure functions tested with no Google present, and all four behaviours are exercised against the fake map — including both orderings of the arrival race. Phase 3 is not finished: the `dot` tier, `MAP_PIN`'s dials and ADR-0122 §7's bottom camera inset remain. Amends 0121 §7 (relates 0122 §9, 0123, 0126 §6) | Accepted |
| [0128](0128-map-dot-tier-and-the-cards-camera-reserve.md) | **The dot tier, and the place card's camera reserve.** The two items standing between Phase 3 and done, both decided on paper and left unbuilt for stated reasons. **ADR-0121 §6's `dot` tier is built:** below `MAP_ZOOM.DOT_BELOW` a pin keeps its hue and drops its glyph, its number and its tip — the tip claims a building and a 30km view cannot honestly claim one. Keyed on **zoom**, not on the canvas, which is the distinction ADR-0123 protected (pin SIZE must not change under a pinch; its TIER may), and done **entirely in CSS off one data attribute the pane writes**, so no marker re-renders and there is no new prop at all — the pane already has the map instance. `zoom_changed`, not `idle`, so it flips during the pinch. It also answers §6's clustering trigger **inside** the pin grammar, where a cluster bubble could not (no hue, no tier, no number). **ADR-0122 §7's bottom camera reserve is built, in a different shape than §7 specified.** The prop question resolves per ADR-0126's build log, plus one thing that boolean did not need: the camera reads it through a **ref**, never a dependency, so the framing effect does not re-run and a pin tap still moves nothing. And the finding that changed the shape — **the card's full band does not fit on any phone at any stop**; an unclamped reserve would have made `fitPaddingFor` drop the whole padding, trading a pin under the card for a pin under the controls row, worse and silent. So the reserve is **clamped to what the axis has left after the top inset**: it degrades rather than switching off, a taller canvas carries more of the card, and it can never cost the top. Also corrects a stale backlog claim: `MAP_PIN`'s dials were **already** calibrated on a real phone (session 143), so what remains there is a case to view, not a number to pick. Phase 3 is closed. Amends 0121 §6 + 0122 §7 (relates 0123, 0126, 0127) | Accepted |
| [0129](0129-map-camera-moves-like-a-camera.md) | **The camera moves like a camera: selection pans, framing is an intent, and nothing portals.** The first Phase-3 feedback from a **rendered canvas** rather than derived, and it reverses part of ADR-0127 the same day it shipped. **(1) Selection pans and never zooms** — being taken somewhere for tapping a pin you can already see is "a little inconvenient", which restores ADR-0121 §7's rule for the two cases it was always right about and is Google's own POI behaviour. ADR-0127's error was not zoom-to-at-least, it was applying it to _selection_. **Zooming becomes an intent with two callers** — an arrival, and the place card's **badge**, which is not a new control: session 148 already made the badge the way to the pin on every other surface (and measured and killed a trailing-slot alternative), so this is the same verb one step further in, reusing `PlaceBadge`. Its corner _marker_ stands down because that corner is the order number's. **(2) How far to zoom is derived from what is nearby**, not from a constant: bounds around the place whose span comes from the distance to its nearest neighbours, fed through the existing fit so it inherits the padding, the card reserve and `MAX_FIT`. Both clamps prevent a real failure (a distant neighbour framing a region; coincident pins snapping to building level), the reach is the furthest of the NEAR ones so a cluster frames as a cluster, and `MAP_ZOOM.PLACE` survives as the no-neighbours fallback. **(3) Nothing portals, and the cause was not a choice** — from the API's own typings, `fitBounds` animates "depending on an internal heuristic", `panTo` only within one viewport, `moveCamera` never. So the ease is ours: `moveCamera` once per frame, one duration for every move, collapsing to a single step under `prefers-reduced-motion`. Three findings from building it: `fitBounds` is now used to **learn** the destination (it owns Google's projection maths) and the camera is restored before easing; the **first** framing lands rather than easing away from a `defaultCenter` nobody chose; and longitude interpolates the short way, because a visible sweep across the world is not the antimeridian case ADR-0121 §14 happily ignores. Amends 0127 §1 + §2 (relates 0121 §7 and its session-148 amendment, 0098 §4) | Accepted |
| [0130](0130-a-maybe-is-not-a-past-place.md) | **A maybe is not a past place: Plan mode has no past, and a maybe is textured rather than faded.** Two owner reports made while using the map, plus the constraint that shaped the third decision ("a normal trip could have tens of maybes but only a handful of maybe-todays"). **(1) The behind-you tier becomes Trip-mode only** — a day you are arranging has no past, and the stops you can least afford to have faded are the ones you came to rearrange; the clock is still passed, because `placeDay` reads it to resolve which day a place is _live_ on, so this is an explicit flag rather than a silent absence. **(2) The real finding: the code drew a maybe/past distinction and it did not work, because both were on the SAME axis** — a maybe was the hue at 55%, a passed stop is the hue desaturated, so the two most different things on the canvas both read as washed out. The design language already had the answer and the pin was the surface that dropped it ("Soft — dashed border, **diagonal-hatch background**"), so the hatch returns and the tint goes: **a maybe keeps its colour and loses its solidity; a past stop keeps its solidity and loses its colour.** Five per-category tint overrides are deleted, not rewritten. **(3) A dayless maybe stops being drawn as another day's business** — ADR-0121 §6 folded "another day" and "no day at all" into one ghost tier, and a place no day has claimed is not busy elsewhere, which is exactly what leaves it available today. Sixth rung: `shelf` wears the maybe's paint at the subordinate ratio, prioritised below today's maybes by size, z-order and the dot tier (the population with tens of pins is the one that degrades at wide zoom). `isAsidePin` names the five call sites that meant "the day scope did not choose this" and only _looked_ like "is a ghost". **(4) The pins were rendered and looked at, and it changed three numbers** — the first hatch read as a barber pole, a maybe visibly louder than a committed stop, which inverts the whole ladder. Amends ADR-0121 §6, ADR-0123's `GHOST_SCALE` → `ASIDE_SCALE`, ADR-0128 §1's day-scope selector. | Accepted |
| [0131](0131-map-search-is-a-control-not-a-screen.md) | **The Map tab's search is a control, not a screen — and the tab becomes where a place is found OR MADE.** Report #18: the tab's search is ADR-0101's full-screen `Modal` `'full'` variant, so on the one tab whose question is _"where is this?"_ the answer renders as a list with the canvas **hidden**. Session 145 traced it as "`query` never reaches `pinsNow`" — every clause true, describing a symptom nobody can observe, because the canvas is not on screen while you type. **(1) The query TAKES the controls row** rather than joining it: the phasing note's "just another control" candidate died on measurement (163.5px spare in Trip mode at 390, but **12.8px in Plan mode + day scope and −17.2px at 360**, where the row already overflows), so it becomes the **second occupant of the disclosure slot `סינון` already covers the row with** — one three-valued state, one `✕`, 44px inside the 46px row, and the split pays no height. **(2) The keyboard is ADR-0101's own reason turned around**: there the results sat _below_ the field, here the canvas is directly under it, so the keyboard eats the **sheet** and the **pins** survive (the whole 250px canvas still on screen at 390×844 `half`, against 0px today). **(3) No match cue — a match is not marked, it is what is left**, a query being a filter like every control in that row; and the ghost trap is answered by ADR-0130 §3's own paint/size split (paint stays, `aside` **ratio** comes off), reaching three of `isAsidePin`'s five readers and deliberately **not** the amber cues or the day connector. **(4) `query` stays OUT of `cameraSignal`**, and not for ADR-0126's reason: a chip is one discrete act, a query is a **stream**, and since ADR-0129 the ease is ours — so typing never moves the camera and committing to a place always does. **Then the owner doubled the phase**, and the correction is the record: _"there are two modes of searching"_, _"how does adding places from events/bookings work"_, _"adding a place by long clicking"_ — all three about **adding** a place, so the draft's Trip/Plan scoping was answering the wrong question. **(5) Two corpora, one control, two surfaces**: the split is **whether the thing has coordinates yet**, so the free half goes on the canvas and the paid half goes into the **sheet** (`PlaceResearch` re-parented, not rewritten) in **both** modes — ADR-0115 §6's "Plan mode only" withdrawn, answered by the arm rather than a mode gate. **(6) A long press drops a nameless pin** — the only add-a-place route that spends nothing (no session, no Details, no reverse geocode), named inside the place card ADR-0122 §7 already built. **(7) `＋ הוספת מקום` answers IN PLACE, and the canvas is the exception path** — the same corpus-decides-the-surface rule one level up: `PlacePickerSheet` gains `בטיול` above `מגוגל`, so picking a place the trip already has assigns it with **no navigation and no spend** (a cost _reduction_, since the picker can only ask Google today), and only "see where it is" or "the place isn't in Google" starts an errand to the tab. On the errand, the draft's "ADR-0090 forbids a remembered return target" overstated it — that ADR names the extension point and `MapScopeProvider`/`useShowPlaceOnMap` already run the hand-over — and the map comes to the tab because a second live map is a **billed** load. That reorders the build: the picker half is the cheap win, the errand follows. **(8) The errand decides the destination, not the source**, which is what keeps three sources from becoming three flows. **Rendering it caught a rule-4 violation the prose could not**: `.map-addmaybe` is `--plan` violet, legitimate while research was Plan-only and mode-identity-as-a-button-colour the moment it renders in Trip mode; it moves to `--cta`. Mockup `map-search-v1.html`. Amends 0101 §2 + 0115 §1/§6 + 0121 §5/§8 + 0122 §1/§2/§7 (relates 0090, 0112, 0120, 0125, 0126, 0128, 0129, 0130). **Built session 158 (§1–§8b) and session 159 (§10's picker half), which also REVERSED §6 — the map extreme is unavailable while a query is live; §9 and the errand remain unbuilt. §2 is amended by 0132: it measured iOS and does not describe Android** | Accepted |
| [0132](0132-search-reclaims-the-chrome-and-a-google-result-is-a-ring.md) | **Search reclaims the app chrome, and an unsaved Google result is a RING, not a pin.** Two device reports on what 0131 shipped, designed together because the second changes the surface the first is laying out — and the first is a correction of 0131 §2's own reasoning: it measured an **iOS** model where the layout viewport does not shrink, while **Android resizes** it, so the shell compresses above the keyboard, the header and nav keep their sizes as fixed content, and the split absorbs the whole loss. Measured from the shipped CSS: **43px of canvas at 390×844, 61px on the owner's 411×914** against §2's claimed 250px — and at 360×640 the pane is **28px** where the controls row + Google's attribution need 68, so the attribution **cannot be laid out**: an **0106 §B violation**, not a density complaint, with a second failure mode on iOS where the pane does not shrink and the keyboard **covers** the attribution instead. That is what makes the reclaim a **condition** rather than a polish pass. **(1) The trigger is the query disclosure being OPEN, not a live query** — the keyboard opens on focus, so `searching` is right for the list and wrong for the chrome. **(2) One modifier in the layout layer** (`display: none`, not `visibility` — the space _is_ the point; nodes stay mounted per 0078's one-frame invariant; no transition), which is the **second consumer** of the `BODY_FULLBLEED` pattern 0121 §5 created for this tab, and therefore **reframes** 0101's July rejection of "a search-mode flag through `AppShell`" rather than contradicting it. **(3) The safe-area insets live on the two nodes being hidden** (`.header` pays `--safe-top`, `.nav` pays `--safe-bottom`), so the modifier pays them on the shell's body and the reclaim is worth **`276 − (top + bottom)`**. **(4) `back` gains one `resolveBack` rule** — a surface that hid the app chrome changed "where am I", so back closes the disclosure before leaving the tab (additive, not an overlay). **(5) The result mark is a silhouette, because the prominence ladder is full** — six tiers, two amber cues, selection, dot tier, hue, fill, grey, hollow, size ratio are all spent, and "not ours yet" is a difference of **kind**, not degree: a **ring** (no tip — a tip claims _which building_; no hue — we do not buy place types; a `＋` — the only verb) that sits **on** the coordinate, off the ladder entirely so nothing composes. The dashed teardrop (collides with the ghost rung, reads as "loading") and the dark inversion (out-ranks the trip's own pins) are drawn and rejected in the mockup. **(6) Text Search, and its cost said plainly:** one call returns N results **with** coordinates, which is what makes pins possible — but against **what ships today** (Autocomplete + one Details on the pick) it may be **more expensive**, since the session token folds a session's requests into one billing when it ends in a pick and Text Search has no session. Per-1000 numbers deliberately unfixed (0108 §3's verify-at-implementation rule); `locationBias` to the canvas bounds is free relevance. **(7) It reopens the map extreme as a question, not as a fact** — half of session 159's reason for closing it dies when results are visible pins, and the condition (a result tap raising `.map-placecard` as a third occupant) plus the remaining gap (a coordless match is still invisible) are named so nobody re-derives or silently undoes it. Mockup `map-google-pins-v1.html`, whose frame is **spliced from the built** `map-search-v1.html` and which draws the keyboard under **both** platform models. Amends 0131 §2 + 0101's Alternatives + 0115 §2 (relates 0078, 0090, 0106 §B, 0108, 0111, 0122 §5/§7, 0126 §1, 0128, 0130, 0017). **Built session 161 in two tiers** (tier 1 the modifier + the safe-area rule + the back layer, which spends nothing and is what clears the ToS failure; tier 2 the SKU switch and the rings). **Five build findings:** the back rule already existed — `useBackLayer`, which `resolveBack` consults first, so `resolveBack` is untouched and nav-state learns nothing about the screen (it gained one `active` flag for a layer whose owner outlives it); the card's third occupant was **not** built because the map extreme is still closed while a query is live, so a ring tap selects its ROW instead (ADR-0122 §7's own rule) and §8's reopening stays owed; the **add got cheaper** and the design had missed it — `resolvePlace` takes client-supplied `details`, so a Text Search add spends **no** Place Details call; `PlaceResearch` became presentational, forced by the SKU (its results are rings now, and a component inside the sheet cannot hand anything to the canvas); and the search bias is a **ref**, because as a value a camera idle would re-bill the query | Accepted |
| [0133](0133-the-user-is-a-surface-identity-ramp-and-a-reachable-roster.md) | **The user is a surface: one settings route, one `Avatar` primitive, an identity ramp, and a roster you can reach.** Three requests in one design session (scoped in the session-159 note), and the code had four things to answer to. **The finding that reframed it:** `avatarColor` is `@default("#E9A63C")` and nothing overrides it on create, so **every real user has the same avatar colour** — the field that exists to tell members apart tells them apart not at all, and only `fixtures.ts` is varied, which is why no screenshot ever showed it. That default is also `--amber`, which `design-language.md:62`'s decorative-palette rule already forbids for avatars: the rule existed, the code violated it, and what was missing was the **ramp** (the doc names the five `--cat-*` pin hues and leaves identity's unnamed, which is how eight call sites invented values — two of them byte-identical to `--cat-transit`/`--cat-lodging`). **(1) One `/settings` route**, user-scoped and not trip-nested, retiring `AccountSheet`; the three shells already reach it, so "from the trip and outside" needed no new affordance. **(2) Back is entry-dependent** — `/settings` is the first shell route with more than one legitimate parent, so the entry writes `?from=` as a **closed enum** (never a path: an open-redirect shape) and `parentRoute` reads it, keeping back a pure function of nav state and surviving reload. **(3) `ui/primitives/Avatar` is extracted FIRST**, as a slice with no screens — eight copies today, eleven after both surfaces. **(4) Source is a stored choice, not a fallback chain** (`avatarChoice`), because someone with a Google photo who prefers a letter must be able to say so; a missing source falls back to initials and never a broken image, which is also the designed **offline** state (the photo is a hotlink; the roster's names, roles and colours still come from the cached snapshot). Google's `picture` needs **no new consent** — the sign-in scope already returns it and `GoogleUserinfo` merely fails to declare it. **(5) The identity ramp is defined by two constraints, not by taste**: outside the semantic trio, and **lower chroma** than the pin hues — because hue angles alone cannot carry it (the palette is crowded, and avatars in the chrome co-occur with pins on the Map canvas), so an identity colour may share an angle and still never read as a category. The default is **derived from `user.id`**, not a column default. **(6) The tap is the choice** — no separate source radio; upload is **absent, not disabled** until Phase 4. **(7) The roster is a statement, not a second management surface** — the cluster becomes a real button (today a `<div title>`: hover-only, so unreachable on a phone, and `MEMBER_AVATAR_CAP = 2` leaves an inert `+2` hiding half the group), listing everyone with role and joined date, while promote/remove/invite stay in trip settings where ADR-0039's server-side gating already lives. **Rendering it against the app's REAL css (the `APP-CSS` manifest) earned its keep three times:** it cut a sixth near-zero-chroma hue that read as a **disabled control**, and it exposed two shipped defects on the elements the roster reuses — `+{n}` renders as `n+` in the RTL chrome (the ADR-0118 isolate bug), and `.role.owner` is amber for a badge that is neither time nor commitment. Phase 3 fixes both rather than propagating them. Deliberately **not** in v1, each with its reason: presence on the roster (already broadcast, dropped client-side), per-member activity, a theme/language/units toggle, a user home zone, account deletion, and broadcasting a rename (`Change` is per-trip while a user spans many). Mockup `user-settings-v1.html`. Ends `app-shell.md` §6's deferral; amends `design-language.md`'s decorative palette (relates 0002, 0005, 0006, 0013, 0017, 0020, 0021, 0028, 0039, 0090, 0094, 0096, 0118) | Accepted (design; build pending) |
| [0134](0134-the-map-is-where-a-forms-place-comes-from.md) | **The map is where a form's place comes from, and a row tap COMMITS.** Three owner requests that are one idea — _you choose a place by seeing where it is_. **(1) The errand becomes the ROUTE**, reversing 0131 §10's own conclusion (which, after four corrections, had made the picker answer in place and the canvas the exception). Recorded as a change of mind, not a re-reading. What reconciles it with the earlier _"don't refer me to the map for a place that already exists"_ is a fact rather than a compromise: the map's free half filters the trip's places **from the first character**, free and offline, so nothing buys a search to find something the trip already holds — and a place is confused with another place **by location**, which is the one thing a list cannot show. **(2) The errand carries a DRAFT**, and that is the cost that moved: as an exception path a lost form was a corner, as the route it is every place choice. `{ target, returnTo, draft }`, the forms gaining a serialise/rehydrate pair and becoming a **third** consumer of the hand-over-and-consume-once channel; `target.field` is **not optional**, because a transport booking has two place fields and without it a successful return assigns the right place to the wrong side. **(3) One pick, and the tap is not the pick** — `בחירה` assigns and returns (no `＋ אולי`, no `MaybeItem`: the owner's "only choosing one place"), while the row tap only **frames**, so you can look before committing, which is the entire reason to be on a map. `נווט` goes absent in errand mode (derived affordance, and it keeps the trip row at 73px instead of growing). **(4) A ROW tap frames; a CANVAS tap pans**, splitting 0129 §1 rather than reversing it: that section's reason (being zoomed for a pin you can already see is a nuisance) is right for a pin and wrong for a row, since a row in a list is the one case where you cannot see the place. The span is **already derived** (`focusBoundsFor`, 0129 §2 — drawn in the mockup as 133×124px against the fixed zoom's 320×297px), so this is two new callers plus one decision: a ring's neighbours are the trip's pins **plus the other rings**, and rings stay out of the camera's `points` because a query must not move it (0131 §5). From `full` the tap drops the sheet to `half` first (0121 §8, one more consumer). **(5) The result row's actions go side by side, measured** — `.map-right` is a column, so a second control costs **height**, the axis 0126 declared scarce: stacked it is 106px and the sheet shows 3 rows at 390×844 / 2 at 360×640, against 68px and 6 / 4 side by side. The Google way-out is an **icon**, and the ADR says plainly that the measurement did **not** decide that (a label fits too) — it rests on the row already carrying one labelled verb. **Retires** `PlacePickerSheet` (the sheet, not the field). **Rejected:** a canvas inside the picker sheet (a second map instance is **billed**, 0121 §4, and from the Map tab that is exactly two), keeping both routes (my recommendation; the owner's reason was better), a tap that commits directly (choosing without looking). Mockup `map-errand-v1.html` — whose own panel measured the PIN instead of the row until it was caught, since both carry `data-place`. Amends 0131 §10 + 0129 §1 + 0115 §2 (relates 0017, 0090, 0112, 0120, 0121 §4/§8, 0122 §7, 0126, 0132) | Accepted (design; build pending) |
| [0135](0135-a-place-becomes-an-event-or-a-booking.md) | **A place becomes an event or a booking: one action on the way-in block.** The epic's remaining product hole: a place added from the map can only ever be an **idea** — `refEntriesFor` builds ways in to references that already exist, and there is no create branch, so nothing said "put this on Tuesday at 14:00". **The session's first design was rejected in place, and both halves of the verdict generalise.** It hung two entries, `＋ אירוע` and `＋ הזמנה`, in the same grey `.map-ref` box as the references; the owner read it as _"cluttered and very amateur looking"_ — correctly, because a `.map-ref` ends in a chevron since it **goes** somewhere and a create goes nowhere, so four equal grey rectangles turned the expanded row into a summary, a navigation list and a **command menu** at once. And: _"maybe the user doesn't choose one or the other but the code understands instead."_ **(1) One action, in a control the tab already owns** — the block keeps its reference list and gains a footer holding `.map-addmaybe`, the same pill `＋ אולי` uses, on the same neutral `--cta`; what separates it from a reference row is shape and weight, not a new colour. One scoped size change, a **floor** rather than a taste: 44×44 here (ADR-0017) instead of the 29px chip a dense result row wants. **(2) What the form then does is NOT this ADR's** — see [0136](0136-an-event-can-also-be-booked.md). Sessions 181-182 designed that here (as a **confirmation code** deciding the entity, a trigger session 184 then **refuted** from the schema: `confirmationCode` is nullable, and a restaurant booking is also soft, so neither signal sees the commonest booking there is); session 183 **extracted §2/§4/§4a/§6** because none of it is map-shaped (owner: _"independently from the maps, events in general"_). This ADR needs exactly one thing from it: that opening the form with a place is enough. **(3) It is not the errand run backwards**, and saying why is most of its value — the errand exists because of **loss** across a round trip between two screens, and there is no round trip here: the form opens over the map on the map's own tab, so `Modal`'s own `useOverlay` is the whole back story. What the errand IS owed is one hook call, since the tab becomes a host of `EventForm`. **(4) The originating idea is consumed** through the path that already consumes it — but **only when there is exactly one**: two ideas on one place are two intentions, and scheduling one must not eat the other. **(5) Absent while an errand is live**, the derived-affordance rule this tab already runs. **(6) Measured:** one control costs **+56px** against the rejected menu's +92px — and the measurement found something neither design caused, that the block already overflows the `half` sheet on a 360 with two references, which is why a `scrollIntoView({ block: 'nearest' })` ships with it. The section numbering keeps its holes on purpose: this ADR is cited by section from the router and the mockup catalog. — **Accepted** [built 2026-07-31, session 185] |
| [0136](0136-an-event-can-also-be-booked.md) | **An event can also be BOOKED, and it is one tap rather than a schema question.** Extracted from [0135](0135-a-place-becomes-an-event-or-a-booking.md), where sessions 181-182 designed it inside a **map** phase, on the owner's call _"independently from the maps, events in general"_ — nothing in it is map-shaped, and filed under the map it would have been unfindable (the exact failure session 180 had just spent a session repairing). Router row: **Data model & events**. Independently buildable, and the backlog carries the two as separate lines. **The reframe is the decision** (owner, session 184: _"many times an event is also a booking"_): they were never alternatives, and the model has never said they were — a `Booking` HAS a linked event, so "this event is also booked" is exactly `event.bookingId != null`. You are **always** creating an event; one `יש הזמנה` toggle says it is **also** booked, which additionally creates the `Booking` and links it. **Two rejected triggers, both instructive.** `＋ אירוע` / `＋ הזמנה` as two entries asks the app's question at the moment a traveller knows least. And a **confirmation code**, which two passes keyed on, is refuted by the schema: `confirmationCode` is nullable and every `Booking` field but `type`/`title` is optional, so a table booked by phone has no number and many people never record one — and there was no fallback signal either, since the commonest booking (a restaurant) is also **soft** by `bookingDefaultKind`. **Booked-ness cannot be inferred, so it is stated**, and inference is demoted to the one honest job it has. **Which is the default:** `lodging` and `transport` open the row **on**, everything else off, and it stops moving once a human touches it (`bookedTouched`, the sibling of the shipped `kindTouched`). The **type** derives from the form's own category via a new `CATEGORY_TO_BOOKING_TYPE`, and the guess is **stated** in a line that moves with the category pill, never asked in a second picker. The booking's own fields (a code, optional, `לא חובה`) appear only once the row is on — the code is a detail OF a booking, never what creates one. **An existing unlinked event converts** by the same toggle: `createBooking` **without** its event seed, then a `bookingId` patch, the two undoing as **one**; the place/category migration is already the server's ADR-0048 invariant. On an event that is **already linked** there is no control at all, only a statement with a way in — which is also what makes it one-way. **And booked-ness is a different axis from commitment:** kind derives from the type at create and is **preserved** on conversion, or switching the row on would silently harden a soft sightseeing event (0011's hard events never auto-move). Costs **+78px** of form on every host for someone who books nothing (482 → 560px, measured), and 642px when booked. Mockup `event-also-booked-v1.html` **Built 2026-07-31 (session 185)**, with §1 and §2 **amended in place**: §1 said "no new primitive, and none needed" and was wrong (the idiom existed four times, hand-rolled in `map.css`, so `ui/primitives/ToggleChip` was extracted and all four Map chips migrated onto it); and §2 promised that a wrong `flight` guess was "one tap on the category", which does not exist — `EventCategory` has a single `transport` while `BookingType` has flight, train and other — so `transport` alone now **asks**, in three pills. — **Accepted** [built 2026-07-31, session 185] |
| [0137](0137-the-pin-says-what-happened.md) | **The pin says what happened there, and the ghost is the population that needed it.** ADR-0117 named three outcome states (`היינו` / `דילגנו` / passed-but-unsettled) and built all three into the LIST, then promised in its Consequences that _"Phase 6 inherits it: the same outcome drives the rendered pin's treatment when the map lands"_. It never did — the canvas shipped one grey `behind` treatment for all three, and `map-pane.css` recorded the gap out loud (_"every behind-you pin looks the same whatever closed it"_), so the row said _we skipped this_ while the pin said only _the clock passed this_. Reported by the owner, whose reading also corrected the population: the tier that needed it is **`ghost`** — a place pencilled for ANOTHER day, hollow by design (no fill, no glyph, no number), which means it has a free centre and the mark costs it nothing. It also earns its keep there, because the only question a ghost raises is _do I still need to care about this?_ **One fact, two homes:** a ghost takes it in the centre as a coloured stroke; a filled `behind` pin **keeps its category glyph** (that glyph is how you tell one grey pin from another — trading it away was this ADR's own rejected first pass) and takes a second badge on the shoulder `.pin-n` leaves free, so the two read as one sentence: where it is in the day, and what happened there. **✓ in `--ok`, ✕ in `--miss`**, which is on-budget rather than an exception (0028 reserves those two for statuses, and 0117's row tags already use them) with shape and the accessible name carrying it independently of colour — reversing v1's confident claim that the canvas could not use the status hues at all: the `saturate(.3)` that would have flattened them lives on `.pin-b`, and moving the mark OUT of the body dissolved the constraint rather than working around it. Every other tier is silent as a **consequence**, not a rule — an idea and a shelf maybe have no event to settle, an ambient night is mid-span, and `upcoming` is unreachable because 0117 §2 already made a human outrank the clock — so Plan mode marks no filled pin while its ghosts still speak. A ghost reports the day it is **live** on (the same resolution `isPlaceLeft` uses), a `behind` pin the day its tier read, and a strictly-middle stay night nothing at all. No schema change, no new tier, no new hue, no second derivation. Build log carries the `dir="auto"` finding that had both badges landing on one shoulder. Leaves one gap, backlogged: the Map can READ an outcome and still cannot WRITE one — **Accepted** |
| [0138](0138-the-row-menu-is-one-surface-and-icons-are-ui.md) | **The `⋯` row menu is ONE surface, and a control's mark is an icon.** Two owner reports on one sheet — emoji drawing verbs, and `הקדם`/`אחר` earning no place — plus three more the design pass found. The menu existed **five times** (`RowManageSheet` ×3, the Plan builder's hand-rolled `.row-actions`, `MemberSheet`'s `.ms-act`), which is why the first two survived: three rulesets for one row shape, already drifted. `ICONS` **splits** into `GLYPH` (content, strings) and `CONTROL_ICON` (`IconName`s) — one object holding both is what made the rule invisible at the call site — and `RowAction.icon` is typed so the four emoji literals no longer compile. Every menu now **names its subject** (the booking and document ones named nothing, while holding the irreversible verb); `danger` **partitions** instead of only recolouring; copy goes imperative. `הקדם`/`אחר` become one **`הזז`** opening a position step that shows where the event lands — reorder is a real capability (`lib/reorder.ts` permutes slots, `ערוך` cannot reproduce it) and dragging is pointer-only. The booking menu gains **`שבץ במסלול`**, the verb its own `לא משובצת במסלול` was asking for. On the owner's call the sweep also crossed the two lines design-language named itself: the **bottom nav** and the **toast/verb-row** marks. Enforced by a lint guard — whose first version inverted the rule, because an astral-emoji character class without the `u` flag is a class of surrogate halves. Amends `design-language.md`; closes the backlog's emoji-sweep item. Leaves one **pre-existing** gap recorded rather than smuggled: `Modal`'s Escape bypasses the back stack. |
| [0139](0139-settling-an-event-from-the-map.md) | **Settling an event from the Map, on the reference that names it.** ADR-0137 gave the canvas the ability to READ an outcome and backlogged what it could not do — WRITE one — so a pin could report `עבר · לא סומן` on a surface with no way to answer it, which is a sharper defect than a missing feature: the marks turned an unsettled stop into a **visible open question** with no answer available. The hard part looked like disambiguation (a place can carry several events on one day, and 0117 §5 forbids collapsing them) and was **already solved**: the way-in block (0121 §8) enumerates the references, one row per entity, each labelled in its own words — so the verb hangs on the row that already names its target and there is nothing to resolve. An idea or a booking gets no controls, because neither carries an `EVENT_STATUS`. **Every event is settleable, not only passed ones**, and the second reason is decisive: gating on "passed and unanswered" would delete the UNDO the instant it was earned. What the clock drives is **emphasis** — an amber wash on a reference whose day passed with nothing said (0117 §1's third state), derived from the same `isDayUsagePast` the pin tier, the block header and `מה נשאר` read, and on-budget because it is a claim about TIME on one row of one selected place; the ✓/✕ stay neutral. Its one structural cost: `.map-ref` stops being a `<button>` and becomes a container with the open affordance as a child and the cluster as a sibling — `ListRow`'s existing shape. **Settling from the PIN is rejected** for the reason that shaped §1 rather than for want of pixels: a pin is a place, an outcome belongs to an event, and a pin cannot say which. The write path is entirely existing (`verbs.done`/`skip`/`restore` → `applySetStatus`, offline via the outbox, undo toast) — a new caller, not a new mechanism, and 0027 §1 holds by construction. Leaves the third hand-rolled settle affordance in the app, which is what earns the shared one: backlogged, vocabulary not geometry — **Accepted** |
| [0140](0140-motion-foundations-overlays-arrive-taps-answer-routes-have-a-direction.md) | **Motion foundations: overlays arrive and leave, taps answer, routes have a direction.** The owner asked for the app to feel better; the mapping pass ([the session-189 brief](../planning/2026-07-31-session-189-motion-and-first-run-brief.md)) found the premise needed correcting — the motion vocabulary exists and is good, and **all of it lives inside a trip**, so the five first-run surfaces had one animation between them. Three foundations, each a primitive rather than a screen. **G1:** `modal.css` animated opacity on the card's own ancestor, so scrim and card cross-faded as one flat image and the card never travelled — and `Modal` unmounted its portal on the frame `onClose` ran, so **no overlay in the app had an exit at all**. The scrim moves to a pseudo-element, each variant arrives the way its shape implies, and the exit hangs off `onClose` (already the one owner of leaving per ADR-0103 §2) rather than adding a second close path; children may take the wrapped close, because an in-card `✕` is the most-used way out. **G3:** `tokens.css` kills the tap-flash app-wide on the stated grounds that every surface has its own `:active` — there were 16 such rules across **seven** different values, so most controls answered a tap with nothing. One element-level rule and two steps (the ratio is what stays constant, not the transform), scale not colour so it needs no per-surface table and spends nothing from the semantic budget. **G2:** direction rides `location.state` stamped by a back that moves — never history (ADR-0090) — finally giving `backSlides` a production consumer, keyed on pathname so an in-trip tab switch keeps its own fade, and RTL-correct off one `--dir` sign. An offset and a fade rather than a full-width push, because the outgoing screen is already unmounted and keeping it would mean two trip shells alive; the receding half is deferred with its cost stated. Adds exactly two vocabulary tokens (`--ease-arrive`, the only overshooting easing, and `--stagger-step`) and leaves the one-cinematic-moment budget untouched. `motionDurationMs` makes reduced motion a correctness case rather than a courtesy — **Accepted**, built the same session |
| [0141](0141-the-pin-says-which-transition-is-next.md) | **The pin says WHICH TRANSITION is next there, and the row already had the word.** Phase 11 of the map panel's third pass, and the framing came from the code: `Map.tsx`'s `dayMeta` already calls `eventEdgeTransition`, and its answer is the FIRST thing a place row's meta line says — so the list read `צ׳ק-אאוט` while the pin above it read `היעד הבא`. This is the **wording half** of what 0137 did for the outcome mark, one object over: one derivation, rendered twice, instead of two halves making unequal claims. **Which end the day sits at carries pre-vs-during**, so no prefix word is minted and no new vocabulary exists at all — `צ׳ק-אין` IS "you have not checked in" and `צ׳ק-אאוט` IS "you are in and the exit is ahead" — and `DayUsage.edge` already leaves a mid-span night silent by construction. Three constraints, three answers: the slot takes **one line** and the word wins it (the two cues stay the fallback, which is all a restaurant reservation has, since 0063 makes `bracketed` 2 of 9 categories and a reservation is one moment); the **amber budget does not move**, because amber stays the two live cues and every other tag is neutral on 0119's own "a pencil mark is stated in a neutral tag" precedent — 0137 §3 having already paid a render for the lesson that _the tokens were right and the amount was not_; and the neutral tag is **day-scoped**, which the mockup forced by refuting the ration carried into it — "the vocabulary is the ration" is true of one day and false of a trip, and all-days has nothing on a pin saying which day the word belongs to, which is 0121 §6's own argument for dropping the number there. An amber pin is exempt because that ambiguity cannot arise on the one place happening now or next. **One carrier was owed and paid:** replacing the word left reduced motion unable to tell `nowstop` from `nextstop` (4.4% of pin height apart, pulse killed globally), so the tag gains `.map-tag.now`'s own leading dot — Home's board blip, completing the "one idiom, three surfaces" `map.css` already claimed. Absorbs the hotel-changing-day follow-up; corrects a `constants.ts` ink-reach number measured on retired copy — **Accepted** |
| [0142](0142-trip-birth-is-the-boards-first-departure.md) | **Trip birth is the board's first departure.** `submit()` succeeding was a React state swap with no motion — the least animated moment in the product and the most significant one. The payoff needed no celebration vocabulary because the app already has one: the zero state renders the departure board **unpowered** (ADR-0024 §2) and Trip Home renders it live, so creation was the **gap between them**. Confetti is drawn in the mockup and rejected — it bolts a second metaphor onto an app that has one, and spends the one-loud-element ration twice on the surface that is already loudest. The board's first row is honest content (the trip's own start date, name and length), its flaps turn with `steps()` because a split-flap is discrete, and it spends **no second `--t-cinematic` moment**: the cinematic asset IS the board's power-on, played at the two moments a trip changes state. The two screens become ONE `.app` root, which is what makes the draft card a **shared element** — one node travelling from the form to the born position, going dashed→solid as ADR-0011's grammar meaning "provisional → committed" at the exact frame the trip stops being a draft. Single-axis transform, because both bodies share their horizontal padding. Plus the CTA **arming** when the form completes, the form assembling on `--stagger-step`, and the invite confirming in place. Skippable by a tap — a celebration you cannot interrupt is a modal dialog wearing a costume — with the skip layer unmounting so it never swallows the tap that copies the link — **Accepted**, built session 190 |
| [0143](0143-the-invite-pass-arrives-and-gets-stamped.md) | **The invite pass arrives, and gets stamped.** `/join/:token` carries the strongest piece of visual writing in the app — a boarding pass with a real perforation, a countdown and the group's avatars — and it was completely static: a bare `<p>` before the preview resolved, a fully-formed pass after it, and `navigate('/')` the instant `joinTrip` returned, so **the moment you were admitted was the one frame the screen did not show**. A pass is an OBJECT: objects arrive, and they get stamped. It settles with a whisper of rotation, its notches **punch** (a hole appears by being made), the avatars land one after another so the row reads as people ARRIVING rather than a row that was always there, and the amber glow now **ramps with the pass** instead of being warm before there is anything to be warm about. The stamp lands on the **server's** success and never optimistically, the pass recoils under it, then it **tears along the perforation the shipped CSS already draws** — decoration that had been promising an action — with the top half (the trip's identity) rising into the handoff and the stub, the part a gate agent keeps, leaving. Loading becomes the pass's own SHAPE, keeping the sentence visually-hidden for anyone who cannot see a shape; an expired invite becomes a **refused** pass rather than a paragraph that reads like a loading state which never resolved; offline deliberately keeps its sentence, being a fact about the network rather than an invitation that failed. **Two corrections to the mockup:** it stamped in **teal**, which ADR-0028 reserves for location (being admitted is a _status_, so `--ok`/`--miss`), and it drew the refused trip struck through, assuming a preview the API does not return for a dead code. `lib/useCountUp.ts` ships shared from the start as the first of a class of beats the brief mapped. **The screen's first tests caught a real bug in this ADR's own mechanism:** arming the tear and handoff timers together stranded the user on a torn pass forever, because advancing to `torn` re-ran the effect and its cleanup cancelled the pending navigation — **Accepted**, built session 190 |
| [0144](0144-the-anchored-panels-arrive-and-leave.md) | **The anchored panels arrive and leave, and the exit machine is shared.** ADR-0140 gave every overlay an arrival and a departure through the one `Modal`; the two surfaces left snapping shut were exactly the two that **deliberately bypass it**. `IconPicker` and `TimePicker` are popovers anchored to their trigger inside a form, not layers over the screen — the same distinction ADR-0121 §5 draws for `SnapSheet` — so folding them onto `Modal` would take them out of the flow they belong in. The fix shares the **mechanism** without the shape: `useExitTransition` extracted from `Modal` and wrapped around each panel's own existing close (IconPicker's three, TimePicker's one), adding no second close path. An anchored panel **unfolds from its edge** (Y-axis scale from `transform-origin: top center`) rather than rising like a sheet or scaling like a dialog — both would lie about where it came from — and the time panel scales rather than animating `height`, which would reflow the form every frame. **The extraction had a latent bug that only a second kind of consumer could expose:** it came out of `Modal`, which unmounts on close, so it never reset — `closing` stayed true and the idempotence guard latched, meaning a panel that persists opens once and can never be shut again — **Accepted**, built session 191 |
| [0145](0145-the-canvas-takes-a-one-finger-zoom.md) | **The canvas takes a one-finger zoom: a double-tap that keeps its finger.** Phase 9 of the map panel's third pass. The phase was triaged on a search snippet, so it was **reconfirmed three ways before anything was designed** — the typings (the whole 45-field `MapOptions` surface has no such flag), the live reference read on a phone, a device (nothing happens), and an independent web-access pass briefed to **falsify** it (CONFIRMED against v3.65 weekly across the guide, `MapOptions`, `<gmp-map>`, `Map3DElement` and ~2 years of release notes). No flag is not no gesture, which is why the device test is the one that closed it. Three precisions came out of the last pass and are recorded rather than banked: Google's web docs **do** carry the words "double-tap and hold … then drag the map" in embedded-map accessibility text (as _navigation_, never as zoom — so the claim is scoped to "not documented as a **zoom** gesture"), **MapTiler inherits the handler from MapLibre** rather than implementing it, and the issue-tracker search is a _non-finding_ behind a sign-in-gated search rather than a negative. It also closed one of the brief's three arbitration options outright: Google exposes **no gesture-handler extension point at all**, so "let Google's handler own it" was never available. **The brief's expected hard part dissolved on measurement:** `.map-pane` is `bottom: var(--sheet-h)`, so the canvas and the sheet's drag region never share a pixel and were never going to fight — they contend only for the finger after it leaves its origin, which pointer capture already settles on both sides. **The real competitor was unnamed: Google's double-tap zoom is enabled**, and the first two taps of this gesture _are_ a double-tap. So the arbitration is a **capture-phase guard**, and "both listen and the loser bails" (ADR-0122 §4's precedent) does **not** transfer — that is safe only because the sheet's loser renders nothing, where **Google's pan writes the camera on the first move** and ADR-0129 §4 would then read the stray pan as the user's own. Blocking Google's double-tap means owing it, so the step-zoom becomes ours — which quietly finishes ADR-0129 §3, since Google's was the one camera move left that could not be asked to ease. **Centre-anchored, not point-anchored**: the tapped point sits under the thumb, holding a pixel needs Google's projection every frame, and ADR-0129 §1 already ruled that a lateral move bundled into a zoom is unwanted on this surface. **Two derivations lost to a finger on the mockup** and both are recorded rather than quietly flipped — the direction (down zooms in; "up means more" from the sheet was a good argument and wrong) and the sensitivity (0.18 → 0.5 over two rounds). The dot tier crossing live costs nothing: ADR-0128 §1's `zoom_changed` data attribute already had exactly this shape — **Accepted**, designed session 193 |
| [0146](0146-the-device-pass-gets-an-instrument.md) | **The device pass gets an instrument, and three of its "numbers" were never numbers.** Every phase since Phase 2 added to the map's device pass and it had never been run — 18 items on one backlog line whose own instruction is _"do them in one sitting on a phone"_, an instruction the code did not allow: each value lives in `constants.ts`, so answering one is edit → rebuild → redeploy → look, and answering eighteen is that eighteen times with no way to hold two candidates side by side. Taken **before** Phase 6b/c on evidence rather than argument: sessions 193–194 shipped Phase 9 and a phone found two of its **models** wrong inside 24 hours — the epic's 4th and 5th device correction of something derived and unit-tested, and in every one the tests asserted what the code did while the model was wrong. **Sorting the cluster is half the deliverable.** Seven are **preferences** and get a live control. Two were never numbers: `half`'s fraction is a **reading** off the drag the sheet already has (a slider there is also the one override that fights 0122 §9, so §9 is neither bent nor argued with — the question was never a slider's), and `MAP_CARD_BODY_H` is a **measurement**, a constant only because 0128 §2 forbids a layout read on a screen that re-renders every second, so the panel measures the rendered card. Five are **look questions** needing no control, carried as a checklist so the sitting records its own answers instead of five judgements from memory. And 0145's centre-anchoring item was **already spent** by §3's amendment the day it was raised — recorded, because a spent item left on a list is how a list stops being believed. **A constant goes live at the READER**, through one accessor handed the constant at the call site, so `constants.ts` keeps the only copy and the seam is visible: every one of the seven is read _inside a function body_, at call time, so a new value is picked up by the next fit, gesture or zoom event with **no prop, no state and no re-render** — which on this surface is the difference between a dev tool and a marker re-diff (cheap) or a re-instantiation (**billed** — 0121 §4, 0122 §9). `MapPane`'s `defaultZoom` deliberately does **not** go through it, being construction-time. Prod contains none of it and that was **run, not asserted**: `tune` survives `pnpm build` as `function I(e,t){return t}` and every panel string is absent from `dist/`. **Steppers, not sliders** — a slider fires tens of events per gesture and cannot land on 0.40 under a thumb, so the expensive shape is designed out rather than guarded, and the chosen value is reportable. The zoom probe is `PinDensity`'s stateless null-rendering shape (0128 §1); the shell is **extracted** from `DevTimeTravel` rather than copied, which also stops two badges landing on each other. The answer leaves as **selectable text**, clipboard only as a bonus: the phone doing the sitting reaches the dev server over http on the LAN, an insecure context where `navigator.clipboard` is undefined. **The invariants are asserted over every value the panel can produce**, not over today's constants, and a broken combination is **stated** rather than made unreachable — narrowing the steppers would make them lie about what the constants can be. What CSS already resolves was free (0123's pin `clamp()`) and turned out **not to be needed**, so it is recorded as one line away rather than shipped unused — **Accepted**, built the same session. **It answers none of the 12 remaining questions**: the sitting needs a real browser key on a real phone, which no agent session has, and saying so is the point |
| [0147](0147-a-place-is-made-on-the-canvas.md) | **A place is made, and named, on the canvas: four sources, one form, and the name is the user's.** Phase 6(b) + 6(c) — the Maps & Places epic's last feature work and its only spending phase — plus the naming/renaming capability the owner added mid-design, which reframed the surface rather than extending it. **A long press · a tap on one of Google's own sights · a search result's add · renaming an existing place are ONE ACT**, because a place's NAME is the user's, and they differ only in what the field starts as. So they share one form (name · the app's own `IconPicker` · nine categories), and only the title, the prefill, the hint and the confirm's word vary. **And it is not a new policy — it is an existing one getting a surface:** `enrichExisting` writes googlePlaceId/address/coords/zone and _deliberately not_ `name`, so a user-authored name has always survived Google enriching the row; `icon` now joins it there. **`Place.icon` is a real column** at the bottom of the resolution chain — under a linked event's deliberate pick and a booking's type glyph, because the deliberate choice at the nearest scope wins and a place is the widest — and **only a PICK is stored**, since a glyph the category derived would freeze the icon and shadow the category from then on. That column also **disqualifies a cross-trip global place cache**, which is wider than the feature and is recorded so it is not rediscovered. The long press is a third phase of ADR-0145's recogniser rather than a second pipeline (which would be a _race_, not merely duplication), the point comes back through Google's own projection in world coordinates, and a POI tap calls `event.stop()` so **Google's card is suppressed and ours is the only one** — which is how ADR-0125 §6's "never two cards" survives the tap becoming ours. The rename affordance is **revealed by selection**, because all three other slots on that row are measured-spent. Amends [0131](0131-map-search-is-a-control-not-a-screen.md) §11 (a fourth source, and a search result's add now opens the form), [0125](0125-map-canvas-terrain-vocabulary.md) §6 and [0110](0110-maps-and-places-frontend-architecture.md) §1. |
| [0148](0148-the-place-form-has-the-room-it-needs.md) | **The place form has the room it needs, one way out of it, and one source fewer.** Three reports off a real phone against the form 0147 shipped the same day. The first looked like one bug and **measuring it made it three**: the card is bottom-anchored and grows upward, so a split shrinking for a keyboard clips it at the **top** — the actions survive and the title and field die, i.e. you can commit a form you cannot read. Two findings neither report contained, both from the mockup's live DOM: at the `full` stop the room is **−38px, constant, keyboard or not**, so the pencil 0147 added opens a form that cannot be drawn in a state that same ADR argued could not arise; and **on iOS every layout number is healthy and the card is invisible**, because the viewport does not shrink and the keyboard is drawn over it (same shape as 0132 §4's iOS attribution failure). The chrome reclaim alone — the obvious reading — is **103px short at `half`**, with the missing part being the field. So: the card is **bounded** by arithmetic over the numbers its own `bottom` already uses, three rows with a pinned head and foot; the form **implies the `map` stop from every origin** and the sheet stands down entirely (`display:none`, +52px), which reverses 0147's rejection on a fact it was argued without and is what makes the pencil work at all; the two quiet lines become **one** (−20px), so the card is **223px for every source**; and the card's `bottom` is `max(sheet+attribution, keyboard)` off `useKeyboardInset` — one hook for both platform models, because **the difference between them IS that number**. The owner then **refused the scroll** (_"some of the form is cut off, this shouldn't happen and I don't want to allow this"_), so the pixels were found instead and the bound survives as a floor for an unmeasured device. The pin now **frames from every source** through a **measured** `cardReserve` rather than a row-sized constant — the fourth time this repo wrote a landing position as a constant. The chrome comes down for the **form's** life, not the keyboard's (a pill tap blurs the field, so a keyboard-literal trigger flickers the shell), through one `chromeReclaimed` with **one writer**. And 6c is **deleted**, not flagged: a form on every POI tap is noise, so 0125 §6 stands unamended and the phase's only paid gesture is gone. Amends 0147 (four places), 0132 §1, 0128 §2 / 0122 §7 | Accepted |
| [0149](0149-the-top-bar-is-two-rows.md) | **The in-trip top bar is two rows, and the exit is lateral.** The header stacked **five rows, one per concern** — mode bar, identity, status, day strip, day-scope ribbon — measuring **250px at rest and 321px off-today** at 390×844 with a notch, i.e. 30–38% of the viewport before the body, and 37–46% of the screen once the tab bar is counted. Three complaints arrived with the height and turned out to be one problem: nobody guesses the trip name is the way out, three adjacent circles do three different things, and **two chrome states push the body when they appear** (the 42px ribbon and the ~30px badges are both in flow), which is most of what read as clanky. So: **two rows** — identity, then the day axis — at **160px in every state, 108px condensed**. The exit is settled against two obvious answers, each rejected on its own grounds: a **menu** improves comprehension _after_ the tap and cannot fix discovery, since you must tap it to discover it; a **back arrow** fixes discovery but **asserts a hierarchy that is false** — 0033's landing rule opens a live trip directly, so trip Home is the main screen and the action is **lateral**, not up. What ships is a **`swap` mark**, visible without a tap, **absent at one trip** (a deck cue behind the glyph shipped with it and was **withdrawn on the device pass** — it read as a box over the flag, not as depth), with the chip still navigating straight to `/trips` so 0033 §3's one-list rule is untouched. **Mode moves to the day row**, icons-only, for a reason that is not space: it **survives the condense and therefore works on the Map**, which opens condensed — a mode switch unreachable on a whole tab is a defect. The ribbon becomes a **fixed-width anchor slot** cross-fading `יום 2/10` ⇄ `היום`, and offline/pending become a **positioned badge**, so nothing in the chrome reflows; `failed` alone promotes to a real `--miss` control, because it is the one sync state a person can act on and 0080 requires a path that never clears. **A surface may declare the condensed chrome as its resting state** — the Map must, since `is-fullbleed` never scrolls, so the scroll trigger is structurally unavailable on the one surface whose scarce axis is height — a third `AppShell` modifier beside `BODY_FULLBLEED` and `CHROME_RECLAIMED`. **Its costs are stated rather than hidden:** the strip narrows 358→182px, so the resting window drops from **7 visible days to 3**. **And measuring it found two shipped defects it did not introduce:** `MAX_TRIP_NAME_LENGTH = 18` exists by its own comment "to keep the pill to one line" and **already fails** — the shipped pill hits `useShrinkToFit`'s 15px floor and clips anyway; and that hook's fit test **compares integers**, so a 1.8px overflow reads as 94 vs 93 and the loop can stop a step early while the browser draws an ellipsis. Rejected and drawn: a one-row bar, and **"the bar comes apart"** (strip back to `DAY_SCOPED_TABS` only, identity as a scrolling page title — Home 44 · Day 102 · Index 44 · **Map 0**), which measures better and was refused for changing the navigation model. Amends 0043 §4/§6, 0033 §3, 0080, 0133 §9 | Accepted (design) |
| [0150](0150-a-form-refuses-at-the-field.md) | **A form refuses at the field, and the field says so.** From an owner report — _"the error for missing fields is nearly noticeable"_ — that reading the code turned into a structural finding: **three** ways to say "this is wrong" and none of them marked the field. A form-level caption above the save button (`EventForm`, `BookingSheet`), which is at the far end of the form from the field it names, does not name it, and — because both forms `return` at the first problem — refuses twice when two things are missing; `Field`'s error slot, right place but caption only (`DocumentUploadSheet`, `MapPlaceForm`); and a hand-rolled `.invalid` class with its own copy of the outline in two stylesheets (`CreateTrip`, `TripSettings`). Shipped: **one hook**, `ui/primitives/useFormErrors`, owning what happens _after_ a refusal — the form still decides what is wrong and in what words — with a field joining by spreading `errors.field(name)`. **Every problem at once**, so two empty mandatory fields are marked together rather than sending the user round the save loop twice. Three marks: the control outlined in `--miss` (a refusal is a **status**, ADR-0028 — nothing new in the palette), **one 240ms nudge** applied imperatively so a repeat attempt at the same field is felt again, and the first problem **in document order** scrolled to centre and focused. Carried by ONE attribute, `data-invalid`, which is what lets a `Field`, a screen's own field div and a single control inside a date range refuse identically — and what let the two hand-rolled `.invalid` rules be deleted rather than migrated. Two findings the build produced: the invalid rule needs its own `:focus-visible` pair, because a refusal focuses the field it names and `field.css`'s teal focus border would otherwise repaint it as healthy (caught in the mockup, invisible to the unit tests); and `EventForm` is now `noValidate`, because the browser's constraint validation was reaching an out-of-trip date first with an untranslated LTR bubble and `submit` never ran. **Swept across every form** — event, booking, place, new trip, settings, document upload — which found a **fourth** shape the audit had not counted, because it looks like an answer rather than a gap: **a disabled primary**, four of them, three saying nothing at all (`TripSettings`' save dead on a four-field `canSave` with no note; `MapPlaceForm`'s confirm dead while its field bound Enter, so `confirm()` ran into a silent `return`; a `fileRequired` string in `DocumentUploadSheet` no path could reach). §8: **disabled means only that a press cannot work** — offline, or a write in flight — never a stand-in for a refusal the button cannot explain. Amends **U-13** accordingly, keeping the create CTA's dim (`:not([data-armed])`) and ADR-0142 §1's arming beat, which was always a `data-armed` animation and never a property of being disabled. Router row: **Platform, design & device targets**. |
| [0151](0151-a-suggestion-has-a-source-and-a-reason.md) | **A suggestion has a source, a score and a reason, and where a strategy runs is a property of the strategy.** Out of the shelf-crowding consultation, where the proposal was a comparator inside a `sort` and the owner reframed it as a recommendations service with pluggable strategies and filters — right about the strategies, and half right about the server. The contract (`{ source, ref, score, reason }`, zod-first in `@waypoint/shared`) and the registry are shared; each strategy declares itself `LOCAL` (no secret, no network, no cost → runs in the browser, works offline) or `REMOTE` (needs a key and bills → runs behind an endpoint whose shape is reserved and **deliberately not built**, because a route whose only implementation is haversine over data the client already holds adds a network dependency to an offline surface for zero capability — and the shelf and gap sheet are exactly the on-the-ground surfaces rule 5 exists for). The first strategy, `near-the-day`, reuses the `haversineMeters` the Map's near-me sort already runs. Four rules carry the growth: scores from different strategies are **not commensurable** (a merge interleaves by rank; normalising them is false precision), a suggestion is **not** a `MaybeItem` (an external candidate owns no row until a human picks it, or the shelf refills with machine volume — the exact crowding being fixed), a paid strategy is **armed by intent** and never fires on rendering a day (ADR-0115's rule one surface over), and every suggestion **states which strategy spoke**, which is what makes a bad ranking arguable rather than magic. |
| [0152](0152-a-note-is-one-entity-with-an-optional-host.md) | **A note is one entity with an optional host, and an external tip is a card until someone keeps it.** Notes lived in exactly one place and it was not a place — `Booking.details.notes`, a key inside a `Json?` blob (ADR-0047 §5), untyped in `@waypoint/shared`, with an `Event`/`MaybeItem`/`Place`/`Document` unable to carry one at all and nowhere to write a general thing the group knows. One `Note` entity instead, where **what it is about is a field**: no host = a general note, a host = that entity's note, and one row/editor/list/sync channel/offline story either way — five `notes` columns would have produced five editors and no unified view, which was the actual request. The host is a **typed nullable FK** with a DB cascade, plus the non-obvious half: a cascade writes no `Change` rows, so the sync guarantee is one rule in the ADR-0094 appliers (a host's delete drops its notes) rather than the database's. Two tiers on one surface, and the boundary is who wrote it: a kept **פתק** is a row, an external tip is a **card that owns no row until a human keeps it** — ADR-0151 §6 one surface over, because a notes list crowds faster than the shelf and the machine never gets bored. `EventCategory` is the category (no open taxonomy: no icon, no exhaustiveness, no LWW merge rule, no i18n), a link is a `url` field rather than a "social media" category, the Index gets its **third tile** on the landing ADR-0098 measured at five, and a note is a **mark** on a row rather than a body in it. Discharges ADR-0098's `מחקר` naming debt by not spending the word, migrates the booking blob's notes (WiFi stays put), and **amends ADR-0151 §2 in place**: placement and cost are two axes, and armed-by-intent keys off cost — Wikipedia is remote and free. **No strategy is registered by this ADR**; §8 is the seam, the same posture 0151 took toward its own endpoint.
| [0153](0153-the-notes-surface-the-mark-and-no-mode-gate.md) | **The notes surface is flat and searchable, a note is a mark on its host, and notes are the one thing with no mode gate.** The design session ADR-0152 owed, closing the brief's §A + §B + §D off two mockups. **The screen is FLAT** — no grouping at all — and the argument outranks the number: there are two jobs here, browsing and targeted lookup, and the second is **not this screen's job** because every host already carries its own notes; grouping by host rebuilds that worse and 22 times over (40 notes fall on 22 hosts, 1.4 rows per header). It needs no general/attached facet either, because `ChoiceGrid` is single-select and the **absence** of a host chip already says it. The tile previews the **newest note with its author** (a note collection has no "next" and no type groups, but it has a newest, and that is the line that changes); the row is a `ListRow` showing three of seven candidate facts, with a title+body note demoting the body rather than printing the sentence twice, a url-only note as an `ltrIsolate` island, and no past-collapse (a note on a past event has not passed). On a host the mark is one `clipboard` silhouette in the meta line, neutral, count only past 1 — and getting it there costs `EventCard` three changes (§6c of 0152), two of which change rows with **no notes at all**: the meta line goes `nowrap` because **flex wraps before it shrinks**, and a row carrying both a confirmation code and a mark **drops its place name** rather than degrading it to a stub. The maybe tile takes it in the **corner** (0px, against the 8px ADR-0151 refused eight days earlier on the same tile). And the one that needed saying out loud: **notes are mode-neutral, with no Plan gate anywhere** — ADR-0025's blast-radius framework makes writing and editing Tier 1, deleting Tier 2 but ungated, and Tier 3 empty; the tempting "a paid strategy is Plan-only" exception is refused on the precedent that ADR-0115 §6's Plan-mode-only rule was already **withdrawn** in favour of arming by cost. Leaves Home + Hero 2.0 open as a stated hand-off rather than a gap.
| [0154](0154-transport-authoring-a-route-shape-a-round-trip-and-a-derived-pair.md) | **Transport authoring: a route is a shape, a round trip is a control, and a pair is derived.** Leaves 0047 §1 alone on purpose — a round trip stays two `Booking`s, because the timeline needs two independent hard events with their own instants, statuses and zones — so nothing here is an entity, a table or a migration. It **corrects 0136 §2**, whose session-185 amendment found that `EventCategory` has one `transport` where `BookingType` has three but not that transport also has a different **place shape**: `EventForm` sends `placeId` with `type: 'flight'` and `assertPlaceShape` **rejects it with a 400**, reproduced rather than inferred, so a transport event with a place cannot be booked at all — and with no place it saves a route-less booking that loses the route title, the location fact, the map pin, the per-endpoint zones, the readiness count and the ability to re-save it. One cause behind both halves: **"is this transport?" is written six times by hand in two packages**, two of them exported in parallel, none exhaustive, and every one really asking "does this carry a route?" — which already costs something shipped, since `🚌 אחר` is transport in the picker and `other` in the model, so a bus can never be given a route. So: §1 the 400 alone, first; §2 `BOOKING_TYPE_PROFILE` beside `CATEGORY_TIME_PROFILE` (`places`/`schedule`/`defaultKind`/`legs`, `legs` deliberately not derived from `places` so a split hotel stay stays expressible); §3 `ui/domain/RouteField` as one component with two hosts, **correcting this design's own first draft**, which copied it instead — plus the swap neither host had; §4 the `הלוך ושוב` pills, **default off** on a measurement (44px on everyone for the control, 492px more only when asked); §5 the pair **derived, not stored**, so it also catches legs written separately or imported, shown as one ink fact (never teal — a sibling is not a location) and one statement, not a fourth button, on delete; §6 the round-trip note on the **outbound**, since today `hostId` would silently take the return by statement order. Index gains nothing, deliberately. |
| [0155](0155-a-stepped-form-is-one-primitive-and-it-commits-once.md) | **A stepped form is one primitive, it never animates height, and it commits once.** Asked because 0154 §4 makes `BookingSheet` ~1565px against ~675px visible on a phone; answered by reading the tree, which found the mechanic **already exists twice and unowned** — `ResolveSheet` and the `הזז` step in `PlanDay.tsx`, each hand-rolling step state and each carrying the same `useBackLayer(…, remainsActive: true)` block whose rule is in `frontend/CLAUDE.md` because it had to be got right twice. Those two are the justification. But they are **branching** steps (step 2 depends on step 1's answer) where a long form is **chunking**, which costs 0150's one-call refusal, 0136's "requires nothing", and reviewing a commitment before signing it. A scan of all nine form surfaces finds **exactly one** candidate, and only after 0154 lands. Two rules the notes work makes non-negotiable and that live in the primitive rather than in each host: it **never animates height** (0152 §6's `max-height: 0 → 220px` clip, and a step panel now holds a composer that grows as `base + N×chip`) and it **commits once, on the last step** (the outbox is FIFO, so a per-step save would queue a note before its host and fail **only offline**). A step gate reports every problem in its own step; the save re-validates all of them and navigates to the first that fails. **Not applied to `BookingSheet`**: it is a genuine middle case — mostly chunking, but 0154's own "the return cannot depart before the arrival" is a real cross-step dependency — so `Collapsible` is tried first and the rest is settled on a phone, not in prose. |
| [0156](0156-a-bus-is-transport-the-third-mode-the-picker-always-meant.md) | **A bus is transport: the third mode the picker always meant.** Closes the gap [0154](0154-transport-authoring-a-route-shape-a-round-trip-and-a-derived-pair.md) §7 stated and left open — `TRANSPORT_BOOKING_TYPES` has offered a `🚌 אחר` pill since the picker shipped, and it wrote `other`, which is **not route-shaped**: a bus saved with a single `placeId`, never got a route field, and could never be given one, losing the route title, the map pin, `ניווט`, the per-endpoint zones and the readiness count with it. Two smaller things in the code were already saying so — the pill had to spell its glyph `🚌` inline because `BOOKING_TYPE_ICON.other` was `📄`, and an `icons.test.ts` assertion pinned the contradiction with a comment naming it. `transit` is a real `BookingType` carrying `TRANSPORT_PROFILE` **verbatim**, which is the first test of 0154 §2's claim that a new mode is one row — and it was. Named `transit`, not `bus`, because the pill covers bus/ferry/car-hire/cable-car and naming it after one of four is the same lie a level down; Hebrew `נסיעה` for the same reason, `תחבורה` being the category's name rather than a booking's. The migration is **additive with no backfill**, deliberately: an existing `other` might be a bus or a theatre ticket and nothing stored distinguishes them, so a guess would re-type real rows — and a wrongly re-typed row would then demand a route it does not have, i.e. a saved booking that cannot be saved again. One behaviour changes visibly: `transit` is a span type, so the third pill now opens **hard** where `other` opened soft, which is 0136 §4 working rather than a special case. |
| [0157](0157-a-place-can-be-removed.md) | **A place can be removed, and the delete says what it costs.** From an owner report — no way in the UI to delete a place, in either mode — which was not a missing button: the `places` module had **no delete at all**, and `host-delete-confirm.test.tsx` said so in prose (_"a `Place` has no delete path in the app to confirm"_). The database already knew what a delete meant — four FKs `SetNull`, the notes `Cascade` — and **neither writes a `Change` row**, which is [0152](0152-a-note-is-one-entity-with-an-optional-host.md) §2's trap over three more stores, so the client derives both locally off the one delete it hears. Referenced places **are** deletable (refusing would mean you cannot clear a place a stale idea holds without deleting the idea first), which buys two obligations: the confirm **names both counts** before it happens, and the undo **restores the place, every FK and every note** — the place first, under its own id, because everything else references it. That last part reaches `createPlaceSchema`, which now accepts `rating`/`userRatingsTotal`: every other field is user-authored or re-derived server-side, and without these two the undo silently returns a place missing its ★. **Two ways in, one confirm.** A trash on the selected row — in the FOOTER, not beside the pencil, on a measurement: the pencil carries its 44px target as an `::after` inset of −14px, so a neighbour would overlap it by ~20px and the one you must not mis-hit is this one — and, because the row and the canvas card are one `renderRow`, it lands on both surfaces from one prop. On the canvas, a **long press on the pin** opens `RowManageSheet` (a fourth consumer after bookings, documents and members), which is what long press means on a phone: it opens options, it does not commit one. That **amends [0153](0153-the-notes-surface-the-mark-and-no-mode-gate.md) §8** — still no kebab in the list, now a gesture-only menu on the surface where the row cannot be shown. The two long presses are kept one gesture rather than two pipelines racing for a pointer ([0147](0147-a-place-is-made-on-the-canvas.md) §1): the recogniser reports **what the finger landed on** and `MapPane` resolves `data-pin`, which also fixes a live defect nobody had filed — a hold over a pin used to drop a second place on top of it. **Amended 2026-08-02 (session 211)** with §6, the orphan sweep ADR-0112 left open — an unreferenced place has no row and no pin, so the delete above cannot reach it and neither can a reader, while every one of them is still downloaded on every cold load. It runs **on a mint** (the only moment the table grows; no scheduler added for a GC), spares anything referenced, anything carrying **notes** (they cascade, and they are visible on the notes screen), and anything touched inside a **week** — a grace doing two jobs, since the row is a paid Places cache _and_ an undo has to find what it re-links to. Its deletions are real `Change`s so peers stay coherent, marked `HOUSEKEEPING_CHANGE` so the feed does not report them against whoever happened to pick a place that minute. §7 records two device corrections: the long press was opening the menu **and** the place card (the release reaches the marker as Google's own callback, which the DOM swallow cannot see — `gestureTapRef` now guards it, one target over from the canvas tap), and the menu's first verb said `שינוי שם` when what it opens is ADR-0147's whole authoring form, so it is `עריכת המקום`. **§8 and §9 (session 212)** are one owner report read twice: the consequence line counted `פריטים`, so deleting a just-added place warned about "one item" — and the item was the **shelf idea the add itself had created**, which no count could have said. So the line **names** its subjects (falling back to counts past two, since a dialog reciting titles stops being a question), and — the owner's own next thought — **a place's sole live shelf idea now goes with it**, which is ADR-0135 §5's consume-on-schedule rule read from the other end, with two-or-more still untouched because two ideas on one place are two intentions. |
| [0158](0158-dark-mode-ships-and-the-ink-a-surface-carries-is-a-token.md) | **Dark mode ships, and the ink a surface carries becomes a token.** The remap was complete and **inert** — every value in `tokens.css`, nothing anywhere setting `data-theme` — so it had never been rendered once. Setting it on the running app found that two of the remap's own recorded conventions are wrong, and that **the shipped LIGHT theme fails contrast 78 times of 238 text nodes against dark's 12** (all twelve one root cause: `.nav` is an `rgba()` the hex-only U-08 sweep never looked at, so the tab bar stays white). Every defect is one shape — **a token whose value was chosen for one of its two roles** — and the repo had already solved it once and never generalised it (`--amber-ink` exists because "dark amber text" and "amber fill" are different jobs). So: **`--on-fill`/`--on-amber`** for ink on a semantic fill, generalising the `--cta`/`--cta-text` pair (ten controls are illegible in dark, worst **1.20:1** — white on near-white on `.choice-pill.on`) — noting that `--indigo` and the board/land/join grounds must NOT join, since dark ink on them measures 1.01:1, two different rules the sweep had written down as one. **Four `--on-dark-*` tokens** folding **18 distinct hex values across 53 sites** on the always-dark surface family, which retires the narrower "mark the ~21 `/login` literals `fixed:`" plan — a token named `--on-dark-dim` states what a comment could only assert — and generalises the `.wp-tzshift.on-dark` one-off already in the tree. **`--plan-surface`/`-2`**, because `--plan-deep` is remapped as _text_ and used as a _fill_, so plan renders the brightest surface in the dark app (L\* 68, ink 2.16:1), **inverting** the mode identity ADR-0028 makes non-negotiable — the root cause being that the trip hero has surface tokens and the plan hero paints itself out of accent ones. **Five `--chrome-*` names**, because 53 chrome declarations assume a dark chrome with only 4 marked, and plan mode already hand-wrote the second copy when its chrome went light. Plus the light theme's own four failures (`--muted`, `--amber-deep` scalar-darkened so hue is untouched; **`--faint` narrowed to placeholders**, because at AA the third ink step lands 0.6 L\* from the second and a three-step ramp is a fiction). The toggle is three rungs, device-local and applied **before first paint** — a server preference cannot beat the first frame, and theme is a property of the screen you are holding, not of the person — which amends ADR-0133 §7's July rejection by its own stated condition. `/login` and `/join` stay theme-**fixed**; the **light trip chrome** is drawn, recommended at indigo 12%, and deferred to Hero 2.0 because it moves a surface ADR-0028 names as mode identity and ADR-0142/0143 are choreographed against. Build phased in §10; every number is a Chromium render and ADR-0125 is why phase 6 is a device pass. |
| [0159](0159-the-day-says-what-is-between-two-events.md) | **The day says what is BETWEEN two events, and a connection is not free time.** Two complaints with one shape. Trip mode's day list drew nothing between two rows while Plan mode drew the same hole as a violet `שבץ` **control** — so Trip takes the same slot with the control removed (a `<span>` where Plan has a `<button>`, `--faint`, no hue), over the same `gapBetween` and the same `GAP_MIN_MINUTES` (one question, two postures), but read through `hoursPhrase` rather than Plan's rounding `gapLabel`: **a statement has to be a measurement**. And a journey with a stop had no way to be authored or read at all — [0154](0154-transport-authoring-a-route-shape-a-round-trip-and-a-derived-pair.md) §7's deferred case, closed on the axis it named. A **connection** is the opposite fact and takes the opposite shape: not a mark between two cards (v1 drew a dotted rail and the owner's phone read killed it — a connector has to touch, and a now-line cut it in half) but **one block containing both legs** with a full-width band between them. Derived, never stored, beside the pair and separated from it by one rule: **a mirror comes back to where it started, a sequence does not** — which is also what stops a through-ticket's shared PNR naming the second half of an outbound journey "the return". `legs` widens to `{ mirrored, sequence }` with a **per-type window** (24h/90m for a flight, 6h/20m for a platform). Authoring: a **stop** on `RouteField` whose `✕` is its remove, and **a leg per step** ([0155](0155-a-stepped-form-is-one-primitive-and-it-commits-once.md) amended), one save writing the chain in one change group with the note on the first leg. The map's pin and row take the same word ([0141](0141-the-pin-says-which-transition-is-next.md) amended). Deferred: legs that fall on different days, and the now-line saying where we actually are. |
| [0160](0160-the-hero-lifts-and-shows-a-horizon.md) | **The hero LIFTS, and what it lifts is a HORIZON.** Closes the Hero 2.0 brief and all six of its sub-questions. The brief called "expansion or overlay?" load-bearing and both answers were wrong: an expansion reflows Home under the thumb, and an overlay as drawn prints the same facts twice and abandons the glance value the board exists for. The owner's own description is the third thing — one object gaining elevation — so this is a **promotion**, built on `.wp-dragghost`'s existing measured-box mechanism, and **still a back layer** (ADR-0103/0090 leave no exemption; what is rejected is the sheet's grammar, not the back contract). What it lifts answers sub-question 1 with a shape rather than a list: the collapsed board shows two **points**, the lifted hero shows a **horizon** with depth on each — and only three things pass "worth an interaction, not a reflow", because the board already has the rest: **איפה** (it has never carried a place at all — Phase 5's affordance, now affordable because it lives in a state you asked for), **פתק** (handed here by ADR-0152/0153) and **הסדרה**. Reads, hand-offs and settles; **no time edits**, so ADR-0011's guard is never reached. **`ועוד N עכשיו` is retired and it is forced, not tidy:** a tappable board is a `<button>` and the expander is a `<button>` inside it — drawn once, Chrome closed the board at the nested button and dropped the divider, `הבא בתור` and the day rail onto the page in dark ink (a probe reports **1 of 4 children left**), so the count becomes a readout and the board ends with no interactive child. The chosen character is **the swing**, and two of the three drawn were **rewritten by measurement**: "rise to under the chrome" measured **4px** (the board is the first thing on Home, so there is nowhere to rise that is not _over_ it — which is why the lifted hero covers the 72px mode-identity band, consistent because the lifted state is modal), and "approach as a scale-up" measured **×1.045** (the board is already near-full-width, so the whole visible budget is height ×2.01 and elevation, and a literal "toward the eyes" has to be a 3D swing). `--t-cinematic` is **unavailable** on ADR-0140's budget rule. The return gets a **landing beat**, making three one-shot answer beats (`.is-nudging`/`.is-rebuffing`/`.is-landing`) that are **one primitive**. The hero is **content-sized** and bounded with one scroller (ADR-0148 §1's pattern), and the fit is measured: the common case is not a scroller, but a **two-way** group split is one at 360×640. A tap with nothing to open gets a **rebuff with no colour and no text**, on a **derived** trigger — a valid `now` board with no note, place, code or sibling has the same nothing to open as a `free` one. Per variant: `in-transit` drops the settle verbs (nonsense, not density — a flight settles itself by landing), `group-split` cannot collapse an equal (ADR-0041 §6 grants it existence on there being no primary), `free` does not lift (`GlanceCard` is already Home's shelf). `SettleControl` gains a **board** density where only the ground changes — `.wp-settle-btn` is `var(--card)`/`var(--ink)`, paper, which is ADR-0158 §15's inverted-surface limit on a new surface. **Design only — not built.** |
| [0161](0161-a-move-names-a-position-and-an-event-owns-its-length.md) | **A move names a POSITION; an event owns its LENGTH.** Four owner reports read together, three of them one defect. `lib/reorder.ts` modelled the day as slots that events permute through, so dropping a 1-hour event on a 2-hour one traded their lengths — while the gap-drop branch of the same drag preserved duration by decision, documented in as many words. So: every move writes a **start** and carries the duration with it; a drop on a **row** swaps positions and a drop on a **seam** inserts, with seams between every pair of rows rather than only where 60 free minutes exist (drawn only while a drag is live). A collision after an unequal swap is answered by ADR-0041's overlap cluster — nothing moves except what the finger touched, which rejects both absorbing the excess into free time and shifting the tail. `שבץ` stops asking for a clock: one picker whose rows are the day's own entries (`אחרי מוזיאון · 12:30`), generalised out of `ResolveSheet`'s before/after pair and `BuilderRow`'s `הזז` peer list, with default lengths from ADR-0063's category profile. `החלף` becomes the atomic Tier-1 decision it always claimed to be, on the same component as the gap fill: the displaced event parks on the shelf and the replacement inherits its exact slot. Amends [0159](0159-the-day-says-what-is-between-two-events.md) §1 — the Trip-mode gap keeps its measurement and gains one tap. Accepted, four slices, building. |
| [0162](0162-a-car-hire-is-transport-you-drive-yourself.md) | **A car hire is transport you drive yourself.** The owner could not find a way to add a rental car. Strictly there was one — ADR-0156 §1 listed car hire among the four things `transit` covers, and `רכב שכור` was already in its search synonyms — but the pill reads `🚌 נסיעה` and a synonym finds a booking you already made rather than telling you the type exists. Underneath, `transit`'s `transportProfile` was wrong for a hire in three ways that only show in use: a **mirrored return leg** (which authors a second _rental_, not a return ticket), a **six-hour connection window** (two hires four hours apart read as one journey with a vehicle change), and `transport`'s **hours** — a five-day hire read "120 ש׳". So `car` becomes the fourth `BookingType` and the **first row that is not a copy of `transportProfile`**: route-shaped (two counters, and both ends may be the same place — 0048's invariant never demanded two) and span-scheduled and hard like the other three, but `legs: ONE_JOURNEY`, which is 0154 §2's `places`/`legs` split finally being needed apart rather than merely claimed. `BookingTypeProfile` gains an **optional** `durationUnit` carrying only the exceptions, and `ICON_TRANSITION_KEYS` widens to `ICON_TIME_PROFILE: Record<string, Partial<CategoryTimeProfile>>` so a glyph refines wording _and_ unit through one merge instead of a second table beside the first. Hebrew is `השכרת רכב` — the act, not the object (owner's call); the car words **move** off `transit` rather than sitting on both, so a bus stops answering a search for a rental. Additive migration, **no backfill** (0156 §3's reasoning, and safer here: both types are route+span, so re-typing an old hire cannot invalidate it). Two things fixed in passing: `timingLabels` became an exhaustive `Record`, which exposed `transit` falling through 0156 to the generic התחלה/סיום where a bus departs and arrives like a train; and `icons.ts`'s private `BOOKING_TYPE_CATEGORY` — a second hand-synced copy of `BOOKING_TYPE_TO_CATEGORY` with one reader, 0095's exact shape — is gone. **Accepted**, built 2026-08-04. |
| [0163](0163-a-hire-is-not-a-journey.md) | **A hire is not a journey.** Four owner reports against the ADR-0162 build, and **three of them are one mistake**: 0162 gave the car hire its own type and left every surface still _describing_ it as a journey. So `BookingSheet` handed it the generic `RouteField` (מוצא → יעד, with a direction swap), and `finalTitle` derived its name from those two endpoints — which titled a same-airport hire **`נריטה ← נריטה`**, and that title is what the day's ambient strip prints. So: `RouteField` gains a `shape` variant asking `איסוף 🔑` plus a same-place/different-place toggle defaulting to SAME, with no swap (you cannot reverse a pick-up and a return) — a variant and not a sibling component, because a copy would fork ADR-0134 §2's per-end errand plumbing. **Nothing in the model moves** (`fromPlaceId` = pick-up, `toPlaceId` = return, and "same" writes them equal so every existing reader stays correct); the toggle is **local state seeded from the props**, because a derived reading cannot express "elsewhere, not yet said where". `Booking.provider` becomes a **form field for every type**, label per type in a `Record` — it was never a model gap: the column and `BookingDetail`'s `ספק` read-out have always existed, and `booking-draft.ts`'s coverage table even documented the assumption as `provider: 'unused'`, so the value was displayable and unenterable at once. **Amends [0059](0059-booking-presentation-on-home-and-index.md) §3** — "a transport booking has no name" is right for a flight and wrong for a hire, so `BookingTypeProfile` gains `titleFrom` and `titlesFromRoute` replaces `carriesRoute` at every title site; separating those two axes is the whole fix, and the fallback with no company is the type label. That deletes the route from every title-only surface at once. The ambient strip counts in the span's own unit (`יום 2 מתוך 5` beside `לילה 2 מתוך 4`), chosen by the owner from three drawn options — the return deadline was the more _useful_ one and lost because the strip's two rows would then say different kinds of thing. Home's mid-stay strip needed the same fix unreported (`שוהים ב־Hertz · לילה 2/5`, wrong on the verb too). Prerequisite done first: `stayNight`/`stayNights` were **three** hand-copied derivations with no tests, now one `ambientSpanPosition`/`ambientSpanLabel` in `lib/glance.ts`. Two things nearly shipped half-done and are recorded as such: the **journey-chain save path** derived each leg's title from its points, so a hire written through `legBooking` kept its route title until the payload test caught it; and the **title-row preview** was still showing 0059 §3's `בחרו מוצא ויעד` ghost, found by reading the rendered sheet. **Accepted**, built 2026-08-04. |
| [0164](0164-a-spans-own-edge-is-something-you-can-still-miss.md) | **A span's own edge is something you can still miss.** Asked about a car hire, answered with nothing to do with cars. Measured first: a same-day flight/train/hire counts **1** in `נותרו היום` and a multi-day hotel or hire counts **0** on every day it covers — one line does all of it (`dayEvents.filter((e) => !isAmbient(e))`), so the hire was never singled out and ADR-0054 was working as designed. What the measurement exposed is the **edge days**: a 15:00 check-in with luggage, an 11:00 check-out, a 10:00 pick-up and a 10:00 return are timed obligations you can breach, and a day whose only real commitment was returning the car read `0 נותרו היום` — the opposite of what the number is for. So `remaining` becomes blocks-still-ahead **plus an ambient span's own edge landing today and not yet reached**. Amends [0077](0077-unified-glance-rail-annotation-grammar.md)'s _"marking a transition is not counting a block"_, which was true of the rail and wrongly applied to the count: **the rail is untouched** (a stay still draws no block and still cannot stretch the window, which is 0054's actual protection) and **middle days still count nothing**, which is what makes this a refinement rather than a reversal. `isAmbient` is the guard against **double-counting**, not a hire-shaped branch — a same-day flight is already a block AND has two anchors, so only the spans 0054 excluded may add themselves back, and a future ambient category inherits the rule for free. Two shipped specs asserted the old number and were rewritten (2→3, 0→1); a third had `uncounted` in its NAME and passed anyway because it never asserted the count, which is the worse failure. Rejected: leaving it (the anchor then says "be out by 11:00" while the number says nothing is left), counting only the END transitions (a check-in is equally missable, and the asymmetry earns nothing), and counting the span once per covered day (re-creates exactly the distortion 0054 prevents). **Accepted**, built 2026-08-04. |
| [0165](0165-a-place-says-what-it-is.md) | **A place says what it is, and the pills that say it write.** From an owner report — _"setting a place category & icon isn't working (it's a no op), no network error or console error"_ — which was two defects, one of them already written down. [0147](0147-a-place-is-made-on-the-canvas.md) §5 recorded that a rename does not persist the category (_"a real limitation … stated rather than hidden"_) and promised the icon carried the tap instead; it did not. A pill tap `redrive`s the glyph, which leaves `iconTouched` **false**, so on a place with no stored glyph — every place that arrived from a search or a drop without a pick — the patch came out **empty**, `applyAuthored` returned early and no request was made. A silent success is what a no-op with no error looks like, and `Map.embedded.test.tsx` pinned it as intended (asserting `updatePlace` was _not_ called). So `Place` gets a nullable `category` column on §5's **own** argument for `icon` — a human's word about a place must survive deleting the idea it was written through — which also answers the ambiguity that blocked it (`soleIdeaFor`: two ideas are two intentions) by having the **place** own the field rather than picking a referencing row. `pin.category` resolves to the place's own over the most-committed reference's, so the hue, the badge glyph and the type facet cannot disagree; `commitment` stays the references' (hard/soft is about the plan), the facet is a **union** (saying what a place is must not take it out of the chip its schedule earned), and an event's own surfaces are untouched (nearest scope wins, and a place is the widest). The seed is **not** a choice: the pills open on the category in force, so the form reports `categoryTouched` beside `iconTouched` through the existing `useDerivedField` and the host writes only on touched-and-changed — without it every typo fix would stamp the derived category onto the row, which is [0136](0136-an-event-can-also-be-booked.md) §2's defect a third time. **"Only a PICK is stored" survives unchanged** and is now load-bearing rather than lossy: the category persists and the glyph is a rendering of it, where storing the derived glyph would re-freeze the icon and shadow the category. Rejected: deleting the pills from the rename form (honest, but a place could then be categorised only in the seconds it was added), persisting the derived glyph alone (the hue still would not follow, so the control stays half-connected), and writing onto the sole referencing idea (the same no-op for an event-referenced place, and the place's category would depend on an idea's lifetime). **Accepted**, built 2026-08-04. |
| [0166](0166-place-enrichment-is-a-multi-source-pipe.md) | **Place enrichment is a multi-source pipe, and what we may CACHE decides its shape.** The owner asked for summaries, images, opening times and ETAs, cached globally. One verified fact reorganized the answer: **Google Places content may not be cached** — `place_id` is exempt and indefinite, coordinates get ~30 days, and names/ratings/photos/phone are request-live-and-attribute, with a photo _name_ not cacheable at all. So the cacheable fields must come from cacheable sources, which inverts the naive design into **open-licensed sources are the own-and-cache tier, Google is the live tier** — meaning multi-source is what makes caching _legal_, not a convenience. The backlog's standing objection (`Place.icon` "disqualifies a cross-trip global place cache") is answered rather than overridden: don't make `Place` global, put a **sibling** global `PlaceEnrichment` (no `tripId`) on the other side of the line the column comment already draws — the trip keeps its _opinion_ (`icon`/`category`/a rename), the global row holds the world's _facts_, and `Place` needs **no migration**. The generalized rule: **if two trips could legitimately disagree about it, it is not enrichment.** Google-free by construction (a testable invariant beats remembering a terms clause, and it is the only version where images work offline) — which knowingly **accepts a coverage hole rather than a defensibility hole**: the café Wikipedia and OSM never heard of gets nothing. A **provider registry with per-field precedence** (summary from Wikipedia, hours from OSM, image from Commons on one place — a source-level winner would discard the best value for every other field), each provider pure, time-boxed, independently failable, and with a **refusable match**, because the real risk is matching rather than fetching: a wrong join silently attaches the wrong photo and hours, which is worse than nothing. Values are provenance-wrapped in one zod-validated JSON payload, not a column per field. Enrichment is **out-of-band from the pick** and deliberately **outside `ChangeService`** (a global row has no `tripId`, no client authors it, and none of LWW/undo/ordering applies) — an argued exception to the one hard boundary, not an oversight. Images take the avatar pipeline as a second consumer, which is `blob-cache.ts`'s first correct reuse after ADR-0108 §3 rightly forbade it _for rows_. **ETA is carved out** — a property of `(origin, destination, mode, departure time)`, traffic-sensitive, already ADR-0108 §4's. Corrects ADR-0108 §3's prediction on both halves; closes ADR-0112's open "related, not decided here"; no code. |
| [0167](0167-the-badge-is-the-thumbnails-frame.md) | **The badge is the thumbnail's FRAME, and selection is where a place says more.** The design pass ADR-0166 §10 gated Phase 1 on, answering all four questions it accumulated. Its whole claim is that **the thumbnail needs no slot**: the row already leads with a 40×40 `r12` badge that is always filled and is already the way to the pin, so a photo fills its interior and the category hue moves from fill to a ring — and rows without an image, which the spike measured as **the majority** (Tokyo restaurants 0 of 7), look exactly as they do today rather than going ragged. **A picked icon beats a fetched photo**, which is ADR-0166 §1's trip's-opinion-vs-world's-facts boundary applied to a 40px square: a background fetch does not get to overwrite a glyph a human chose. Enrichment lands in `.map-refs`, the reveal that already exists, in one fixed order (hero → credit → summary → hours → way-through), so an unselected row pays nothing — the bargain `.map-rename` already struck. **Attribution renders on the card under the image**, never overlaid (it would fight a bright sky and need re-solving in dark) and never on the badge (a credit line cannot exist in a 73px row) — and since 27 of 32 files require it, it is the default line rather than an exception. `עוד בגוגל` sits in the reveal's footer, is never `ניווט`, and **is the entire content of the card when we know nothing**, which is the common case. **Two defects the mockup produced by being rendered, both of which would have shipped:** an `inset` box-shadow paints below the element's children, so the photo covered the hue ring and the badge silently lost its category; and the credit line, being Latin, made itself LTR under `dir="auto"` and aligned to the opposite edge from every other line in the card — the **mirror** of ADR-0118's bug, invisible to its lint guard, and it will recur on every enriched Latin string. One measurement also reversed a plan: the hero looked like the card's height lever and is not (132 → 96px moves it only 392 → 356px), so it keeps its size. Collapsed rows measured 69–71px, so the frame genuinely costs nothing. Design only, nothing built; the one thing it cannot answer is whether a real photograph is legible at 40px. |
| [0168](0168-the-search-answers-on-the-canvas.md) | **The search answers on the canvas, the result mark stops borrowing the basemap's palette, and a second tap closes what the first opened.** Five device-pass reports on one surface, two of which correct reasoning rather than pixels. **(1)** [0131](0131-map-search-is-a-control-not-a-screen.md) §5 kept the query out of the camera because "a chip is one discrete act where a query is a stream" — true of a **keystroke** and false of a **settled Text Search response**, which is one discrete fact already gated by the min-chars floor and the pause debounce. So a settled result set moves the camera, through one pure function with four rules: nothing when they are all already on canvas (**the anti-jitter rule** — consecutive queries in one neighbourhood move nothing), **pan** at the zoom you are on when the extent fits it, **fit** when it does not but the set is still one area, and past `SPREAD_CAP_DEG` **the top-ranked result framed among its own cluster** — because `דואומו` across four Italian cities fitted whole is a country view of four specks, which answers the owner's own open question with a no. It deliberately does **not** reuse `boundsFillView`: re-fitting a set that is on screen but small in it is right for a **filter** and is the unasked-for zoom [0129](0129-map-camera-moves-like-a-camera.md) §1 removed from a pin tap. **(2)** The `＋`-in-a-circle read as "out of place and amateur, especially in light mode", and the reason is not taste: `design/map-styles/waypoint-map-day.json` paints Google's **own** POIs `#c9ccd4` fill · `#4b5568` glyph · `#ffffff` outline, which is the shipped ring's palette redistributed — so the mark read as **basemap furniture**, and no tuning of greys could fix it because the basemap owns the greys. The same file explains "in dark theme it's better": at night that POI is `#414b61` on `#191e2c`, so the ring did not improve, **its competition got quieter**. Redrawn in the canvas's own marker grammar (a fill, a `--card` keyline at `.pin-b`'s ratio, its shadow) in **teal** — not a new hue and not a borrowed one: [0028](0028-plan-violet-color-budget-dark-ready.md) reserves teal for location, the category palette may **never** be teal, and a Google result is the one object here that is _only_ a location, so a sixth category cannot collide with it later. The `＋` goes because it was illegible at 28px **and** named the wrong gesture (a ring tap selects; the add is a labelled control on the row it raises); the hole stays, since silhouette is the axis [0132](0132-search-reclaims-the-chrome-and-a-google-result-is-a-ring.md) §6 spent. Selection now **closes the hole**, grows and haloes, keeping the hue — a `--cta` selection was drawn and rejected for looking like a different kind of object than its own resting state. **(3)** The reveal's scroll animates (the offset was already right; an instant arrival just left the list somewhere else), and the ring tap's second, `center`-aligned copy of that job is retired into the one helper. **(4)** A tap on a row's body has three readings, innermost first: expanded → back, selected → close, neither → select — so `‹ חזרה לפרטי המקום` keeps its name and the whole card becomes its target, which the canvas card inherits for free. The sharp edge: a row can be selected by a **pin or ring**, which only pan, so the row's own tap is [0134](0134-the-map-is-where-a-forms-place-comes-from.md) §6's _framing_ gesture — closing on any second tap deleted it, and one existing test caught that. A row closes only once its own tap opened it: ring → pan, row → frame, row → close. **(5)** "Tapping what is already selected commits it" (0134's session-171 addendum) stops being errand-scoped for a **ring**: it was gated on "outside an errand there is nothing to commit to", and there is — the shelf. One gesture, the context picks the destination, and it skips the naming form deliberately. Still the already-selected tap rather than a `dblclick`, because on this canvas the double tap is the step-zoom. |
| [0169](0169-the-app-answers-on-one-host.md) | **The app answers on one host, and the invite link reads as one.** A bought domain is two names, and this app's session is a **host-only cookie** (ADR-0020) — so `travelive.app` and `www.travelive.app` are two logins that cannot see each other. The report was a **single mistyped character** (`FRONTEND_URL` = `https://wwww.…`, four `w`s): login succeeded and the browser was then sent to a hostname that does not exist, with a green healthcheck and nothing in the logs. Worse, a Google round-trip started on one host while `GOOGLE_OAUTH_REDIRECT_URI` names the other returns to a callback that cannot verify itself and signs the user out in silence. One host is canonical (`FRONTEND_URL`, already the post-login redirect target), every other host is 302'd there by an express middleware **before routing** with its path and query intact — `/health` exempt, because a redirected healthcheck kills the deploy, and `GET`/`HEAD` only, because a redirected `POST` loses its body. A `FRONTEND_URL`/`GOOGLE_OAUTH_REDIRECT_URI` **host split now refuses to boot** (ADR-0071), since that pair cannot log anyone in. The invite becomes two strings — the url that gets pasted and a label that reads `travelive.app/join/7Kq2mB`, through `prettyUrl`, which already answered "how does a url read" for notes. §5 records what DNS allows: Railway needs a CNAME, an apex CNAME needs flattening, **GoDaddy has none** — so apex-canonical means moving nameservers, and the cheap alternative (apex on GoDaddy forwarding) silently eats the path out of a pasted `/join/` link | Accepted |
| [0170](0170-the-product-is-travelive-the-codebase-is-waypoint.md) | **The product is Travelive; the codebase stays Waypoint.** The working codename got a bought name and a bought domain, and the rename stops at what a person reads: the two wordmarks, the `<title>` and the PWA manifest, all four reading one import-free `app-name.ts` (`index.html` via a `%APP_TITLE%` substitution, since the browser reads it before the bundle exists). **Every identifier stays `waypoint`** — and not only out of laziness: `new Dexie('waypoint')` and the `waypoint:*` storage keys ARE the identity of each user's offline cache, so "finishing" the rename doesn't migrate it, it points the app at an empty one and wipes every member's cache and theme on the deploy that ships it | Accepted |
